/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
If you want to view the source, please visit the GitHub repository of this plugin.
*/

var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/main.ts
var main_exports = {};
__export(main_exports, {
  default: () => Dashboard
});
module.exports = __toCommonJS(main_exports);
var import_obsidian6 = require("obsidian");

// src/settings.ts
var import_obsidian = require("obsidian");
var HOME_LAYOUT_VERSION = 3;
var HOME_MODULE_TITLES = {
  "todo": { zh: "\u5F85\u529E \xB7 \u4ECA\u5929\u8981\u5904\u7406", en: "To-do \xB7 Today" },
  "weather": { zh: "\u5929\u6C14", en: "Weather" },
  "calendar": { zh: "\u65E5\u5386", en: "Calendar" },
  "sites": { zh: "\u7F51\u7AD9\u6536\u85CF", en: "Bookmarks" },
  "favorites": { zh: "\u5E38\u7528\u6587\u6863", en: "Favorites" },
  "quick-capture": { zh: "\u7075\u611F\u6355\u6349", en: "Quick Capture" },
  "pomodoro": { zh: "\u756A\u8304\u949F", en: "Pomodoro" }
};
var DEFAULT_SETTINGS = {
  banner: { imageDataUrl: null, offsetY: 0 },
  quickCapture: {
    storagePath: "Dashboard/Temp",
    namingPattern: "YYYY-MM-DD HH-mm \u6355\u6349",
    templateFile: ""
  },
  diary: {
    storagePath: "Daily",
    namingPattern: "YYYY-MM-DD",
    templateFile: ""
  },
  todoSourceFolder: "",
  projectsFolder: "Projects",
  theme: "auto",
  language: "zh",
  dashboardTitle: "",
  homeLayoutVersion: HOME_LAYOUT_VERSION,
  countdown: { eventName: "2027", targetDate: "2027-01-01" },
  pomodoro: {
    workMinutes: 25,
    shortBreakMinutes: 5,
    longBreakMinutes: 15,
    cycles: 4,
    autoStart: false,
    sound: true,
    dailyCount: 0,
    dailyDate: ""
  },
  favoriteDocs: [],
  weather: { cities: ["\u65E5\u7167"] },
  favoriteSites: [],
  kanbanFile: "Dashboard/\u5F85\u529E\u4E8B\u9879.md",
  favoriteSitesFile: "Dashboard/\u7F51\u7AD9\u6536\u85CF.md",
  favoriteDocsFile: "Dashboard/\u5E38\u7528\u6587\u6863.md",
  // 默认布局（与产品截图一致，4 列网格）：
  // 待办 / 网站收藏 为高卡（1×2），日历 / 常用文档 / 快速捕捉 / 番茄钟 为矮卡（1×1）；
  // 天气默认隐藏（截图未含，需要时到「首页模块」打开）。
  homeModules: [
    { id: "todo", enabled: true, order: 0, cols: 1, rows: 2 },
    { id: "calendar", enabled: true, order: 1, cols: 1, rows: 1 },
    { id: "sites", enabled: true, order: 2, cols: 1, rows: 2 },
    { id: "favorites", enabled: true, order: 3, cols: 1, rows: 1 },
    { id: "quick-capture", enabled: true, order: 4, cols: 1, rows: 1 },
    { id: "pomodoro", enabled: true, order: 5, cols: 1, rows: 1 },
    { id: "weather", enabled: false, order: 6, cols: 1, rows: 1 }
  ]
};
var DEFAULT_HOME_MODULES = [
  { id: "todo", enabled: true, order: 0, cols: 1, rows: 2 },
  { id: "calendar", enabled: true, order: 1, cols: 1, rows: 1 },
  { id: "sites", enabled: true, order: 2, cols: 1, rows: 2 },
  { id: "favorites", enabled: true, order: 3, cols: 1, rows: 1 },
  { id: "quick-capture", enabled: true, order: 4, cols: 1, rows: 1 },
  { id: "pomodoro", enabled: true, order: 5, cols: 1, rows: 1 },
  { id: "weather", enabled: false, order: 6, cols: 1, rows: 1 }
];
var DashboardSettingTab = class extends import_obsidian.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
  }
  /** 按当前语言返回中 / 英文案 */
  s(zh, en) {
    return this.plugin.settings.language === "en" ? en : zh;
  }
  /** 首页模块 id → 按当前语言的显示名称 */
  modName(id) {
    const m = HOME_MODULE_TITLES[id];
    return m ? this.plugin.settings.language === "en" ? m.en : m.zh : id;
  }
  display() {
    var _a, _b;
    const { containerEl } = this;
    containerEl.empty();
    new import_obsidian.Setting(containerEl).setName(this.s("\u6570\u636E\u5B58\u50A8\u4F4D\u7F6E", "Storage Paths")).setHeading();
    new import_obsidian.Setting(containerEl).setDesc(this.s("\u4EE5\u4E0B\u4E3A\u5404\u6A21\u5757\u5B58\u50A8\u5185\u5BB9\u7684 Markdown \u6587\u6863 / \u6587\u4EF6\u5939\u8DEF\u5F84\uFF0C\u7EDF\u4E00\u5728\u6B64\u4FEE\u6539\uFF1B\u4FEE\u6539\u5373\u65F6\u4FDD\u5B58\uFF0C\u65E0\u9700\u91CD\u542F\u3002", "Paths for each module Markdown data file. Changes apply immediately, no restart needed."));
    const addPath = (name, desc, placeholder, get, onChange) => {
      new import_obsidian.Setting(containerEl).setName(name).setDesc(desc).addText(
        (t) => t.setPlaceholder(placeholder).setValue(get()).onChange((v) => void onChange(v.trim()))
      );
    };
    addPath(
      this.s("\u5F85\u529E \xB7 Kanban \u6570\u636E\u6587\u4EF6", "To-do \xB7 Kanban file"),
      this.s("\u5F85\u529E\u5361\u7247\u8BFB\u5199\u7684 Obsidian Kanban \u63D2\u4EF6\u6570\u636E\u6587\u4EF6", "Kanban data file read and written by the to-do card"),
      "Dashboard/\u5F85\u529E\u4E8B\u9879.md",
      () => this.plugin.settings.kanbanFile,
      async (v) => {
        this.plugin.settings.kanbanFile = v || "Dashboard/\u5F85\u529E\u4E8B\u9879.md";
        await this.plugin.saveSettings();
        this.plugin.refreshHome();
      }
    );
    addPath(
      this.s("\u7F51\u7AD9\u6536\u85CF \xB7 \u6570\u636E\u6587\u4EF6", "Bookmarks \xB7 data file"),
      this.s("\u300C\u7F51\u7AD9\u6536\u85CF\u300D\u5361\u7247\u8BFB\u5199\u7684 Markdown \u6587\u4EF6\uFF08## \u5206\u7C7B + - \u540D\u79F0 \u2192 URL\uFF09\uFF0C\u53EF\u76F4\u63A5\u7F16\u8F91\uFF0C\u4E0E\u5361\u7247\u53CC\u5411\u540C\u6B65", "Markdown file for the bookmarks card (## category + - name \u2192 URL). Editable directly, two-way synced with the card"),
      "Dashboard/\u7F51\u7AD9\u6536\u85CF.md",
      () => this.plugin.settings.favoriteSitesFile,
      async (v) => {
        this.plugin.settings.favoriteSitesFile = v || "Dashboard/\u7F51\u7AD9\u6536\u85CF.md";
        await this.plugin.saveSettings();
        this.plugin.refreshHome();
      }
    );
    addPath(
      this.s("\u5E38\u7528\u6587\u6863 \xB7 \u6570\u636E\u6587\u4EF6", "Favorites \xB7 data file"),
      this.s("\u300C\u5E38\u7528\u6587\u6863\u300D\u5361\u7247\u8BFB\u5199\u7684 Markdown \u6587\u4EF6\uFF08- [[\u8DEF\u5F84|\u663E\u793A\u540D]]\uFF09\uFF0C\u53EF\u76F4\u63A5\u7F16\u8F91\uFF0C\u4E0E\u5361\u7247\u53CC\u5411\u540C\u6B65", "Markdown file for the favorites card (- [[path|title]]). Editable directly, two-way synced with the card"),
      "Dashboard/\u5E38\u7528\u6587\u6863.md",
      () => this.plugin.settings.favoriteDocsFile,
      async (v) => {
        this.plugin.settings.favoriteDocsFile = v || "Dashboard/\u5E38\u7528\u6587\u6863.md";
        await this.plugin.saveSettings();
        this.plugin.refreshHome();
      }
    );
    addPath(
      this.s("\u7075\u611F\u6355\u6349 \xB7 \u5B58\u50A8\u8DEF\u5F84", "Quick capture \xB7 storage path"),
      this.s("\u300C\u7075\u611F\u6355\u6349\u300D\u65B0\u5EFA\u7B14\u8BB0\u7684\u5B58\u653E\u6587\u4EF6\u5939", "Folder where quick-capture notes are created"),
      "Dashboard/Temp",
      () => this.plugin.settings.quickCapture.storagePath,
      async (v) => {
        this.plugin.settings.quickCapture.storagePath = v || "Dashboard/Temp";
        await this.plugin.saveSettings();
      }
    );
    new import_obsidian.Setting(containerEl).setName(this.s("\u9996\u9875\u6A21\u5757", "Home Modules")).setHeading();
    new import_obsidian.Setting(containerEl).setDesc(this.s("\u5728\u6B64\u7BA1\u7406\u9996\u9875\u5361\u7247\uFF1A\u663E\u793A / \u9690\u85CF\u3001\u8C03\u6574\u987A\u5E8F\u4E0E\u5927\u5C0F\u3002\u4FEE\u6539\u5373\u65F6\u4FDD\u5B58\u5E76\u5237\u65B0\u9996\u9875\u3002", "Manage home cards: show / hide, order and size. Changes apply immediately and refresh the home page."));
    const hmWrap = containerEl.createDiv();
    const renderHmList = () => {
      var _a2;
      hmWrap.empty();
      const hms = [...(_a2 = this.plugin.settings.homeModules) != null ? _a2 : []].sort((a, b) => a.order - b.order);
      hms.forEach((m, idx) => {
        const row = new import_obsidian.Setting(hmWrap).setName(this.modName(m.id));
        row.addToggle(
          (t) => t.setTooltip(this.s("\u663E\u793A / \u9690\u85CF\u6B64\u5361\u7247", "Show / hide this card")).setValue(m.enabled).onChange(async (v) => {
            m.enabled = v;
            await this.plugin.saveSettings();
            this.plugin.refreshHome();
          })
        );
        row.addButton((b) => b.setButtonText("\u2191").setTooltip(this.s("\u4E0A\u79FB\uFF08\u8C03\u6574\u4F4D\u7F6E\uFF09", "Move up")).onClick(async () => {
          if (idx === 0) return;
          const prev = hms[idx - 1];
          [m.order, prev.order] = [prev.order, m.order];
          await this.plugin.saveSettings();
          this.plugin.refreshHome();
          renderHmList();
        }));
        row.addButton((b) => b.setButtonText("\u2193").setTooltip(this.s("\u4E0B\u79FB\uFF08\u8C03\u6574\u4F4D\u7F6E\uFF09", "Move down")).onClick(async () => {
          if (idx >= hms.length - 1) return;
          const next = hms[idx + 1];
          [m.order, next.order] = [next.order, m.order];
          await this.plugin.saveSettings();
          this.plugin.refreshHome();
          renderHmList();
        }));
        row.addDropdown((d) => {
          var _a3;
          for (let c = 1; c <= 4; c++) d.addOption(String(c), this.s("\u5BBD " + c, "W " + c));
          d.setValue(String((_a3 = m.cols) != null ? _a3 : 1));
          d.onChange(async (v) => {
            m.cols = parseInt(v) || 1;
            await this.plugin.saveSettings();
            this.plugin.refreshHome();
          });
        });
        row.addDropdown((d) => {
          var _a3;
          for (let r = 1; r <= 4; r++) d.addOption(String(r), this.s("\u9AD8 " + r, "H " + r));
          d.setValue(String((_a3 = m.rows) != null ? _a3 : 1));
          d.onChange(async (v) => {
            m.rows = parseInt(v) || 1;
            await this.plugin.saveSettings();
            this.plugin.refreshHome();
          });
        });
      });
      new import_obsidian.Setting(hmWrap).setName(this.s("\u6062\u590D\u9ED8\u8BA4\u5E03\u5C40", "Reset to Default")).setDesc(this.s("\u5C06\u9996\u9875\u5361\u7247\u7684\u663E\u793A / \u9690\u85CF\u3001\u987A\u5E8F\u4E0E\u5927\u5C0F\u5168\u90E8\u91CD\u7F6E\u4E3A\u9ED8\u8BA4", "Reset all home cards to the default show / hide, order and size")).addButton((b) => b.setButtonText(this.s("\u6062\u590D\u9ED8\u8BA4", "Reset")).onClick(async () => {
        await this.plugin.resetHomeLayout();
        this.plugin.refreshHome();
        renderHmList();
      }));
    };
    renderHmList();
    new import_obsidian.Setting(containerEl).setName(this.s("\u5FEB\u901F\u6355\u6349", "Quick Capture")).setHeading();
    new import_obsidian.Setting(containerEl).setName(this.s("\u6587\u4EF6\u547D\u540D\u89C4\u5219", "File Naming Pattern")).setDesc(this.s("\u652F\u6301\u53D8\u91CF\uFF1AYYYY \u5E74\u3001MM \u6708(2\u4F4D)\u3001MMM \u6708\u7F29\u5199(\u5982 8\u6708)\u3001DD \u65E5\uFF1Bddd \u5468\u65E5\u3001dddd \u661F\u671F\u65E5\uFF1BHH 24\u65F6\u3001hh 12\u65F6\u3001mm \u5206\u3001ss/SS \u79D2\u3001A \u4E0A\u5348/\u4E0B\u5348", "Variables: YYYY year, MM month (2-digit), MMM month, DD day, ddd weekday, HH 24h, hh 12h, mm minute, ss/SS second, A AM/PM")).addText(
      (t) => t.setPlaceholder("YYYY-MM-DD HH-mm \u6355\u6349").setValue(this.plugin.settings.quickCapture.namingPattern).onChange(async (v) => {
        this.plugin.settings.quickCapture.namingPattern = v;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.s("\u6A21\u677F\u6587\u4EF6", "Template File")).setDesc(this.s("\u8F93\u5165\u6A21\u677F\u8DEF\u5F84\uFF0C\u4E0D\u4F7F\u7528\u6A21\u677F\u5219\u4E3A\u7A7A", "Template path; leave empty to use none")).addText(
      (t) => t.setPlaceholder("Templates/\u901F\u8BB0.md").setValue(this.plugin.settings.quickCapture.templateFile).onChange(async (v) => {
        this.plugin.settings.quickCapture.templateFile = v.trim();
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.s("\u5F85\u529E \xB7 \u4ECA\u5929\u8981\u5904\u7406", "To-do \xB7 Today")).setHeading();
    new import_obsidian.Setting(containerEl).setName(this.s("\u8BF4\u660E", "Note")).setDesc(this.s("\u6570\u636E\u6587\u4EF6\u8DEF\u5F84\u8BF7\u5728\u9876\u90E8\u300C\u6570\u636E\u5B58\u50A8\u4F4D\u7F6E\u300D\u7EDF\u4E00\u914D\u7F6E\uFF1B\u6B64\u5904\u4FDD\u7559\u547D\u540D\u4E0E\u5C55\u793A\u76F8\u5173\u8BBE\u7F6E\u3002", "Configure the data file path in Storage Paths at the top; only display-related settings are here."));
    new import_obsidian.Setting(containerEl).setName(this.s("\u756A\u8304\u949F", "Pomodoro")).setHeading();
    new import_obsidian.Setting(containerEl).setDesc(this.s("\u4E13\u6CE8 / \u4F11\u606F\u65F6\u957F\uFF08\u5206\u949F\uFF09\u3002\u4FEE\u6539\u5373\u65F6\u751F\u6548\uFF0C\u65E0\u9700\u91CD\u542F\u3002", "Focus / break durations in minutes. Changes apply immediately, no restart needed."));
    const addNumSetting = (name, desc, min, max, get, onChange) => {
      new import_obsidian.Setting(containerEl).setName(name).setDesc(desc).addText(
        (t) => t.setPlaceholder(String(get())).setValue(String(get())).onChange((v) => {
          let n = parseInt(v, 10);
          if (!Number.isFinite(n)) n = min;
          n = Math.max(min, Math.min(max, n));
          onChange(n);
        })
      );
    };
    addNumSetting(
      this.s("\u4E13\u6CE8\u65F6\u957F", "Focus duration"),
      this.s("\u5355\u4E2A\u756A\u8304\u7684\u4E13\u6CE8\u65F6\u957F\uFF081\u2013180 \u5206\u949F\uFF09", "Length of one focus session (1\u2013180 min)"),
      1,
      180,
      () => this.plugin.settings.pomodoro.workMinutes,
      async (v) => {
        this.plugin.settings.pomodoro.workMinutes = v;
        await this.plugin.saveSettings();
        this.plugin.refreshHome();
      }
    );
    addNumSetting(
      this.s("\u77ED\u4F11\u606F", "Short break"),
      this.s("\u6BCF\u4E2A\u756A\u8304\u4E4B\u95F4\u7684\u77ED\u4F11\u606F\uFF081\u201360 \u5206\u949F\uFF09", "Short break between pomodoros (1\u201360 min)"),
      1,
      60,
      () => this.plugin.settings.pomodoro.shortBreakMinutes,
      async (v) => {
        this.plugin.settings.pomodoro.shortBreakMinutes = v;
        await this.plugin.saveSettings();
      }
    );
    addNumSetting(
      this.s("\u957F\u4F11\u606F", "Long break"),
      this.s("\u8FDE\u7EED\u5B8C\u6210\u82E5\u5E72\u756A\u8304\u540E\u7684\u957F\u4F11\u606F\uFF081\u201390 \u5206\u949F\uFF09", "Long break after several pomodoros (1\u201390 min)"),
      1,
      90,
      () => this.plugin.settings.pomodoro.longBreakMinutes,
      async (v) => {
        this.plugin.settings.pomodoro.longBreakMinutes = v;
        await this.plugin.saveSettings();
      }
    );
    addNumSetting(
      this.s("\u957F\u4F11\u606F\u95F4\u9694", "Long break interval"),
      this.s("\u6BCF\u5B8C\u6210\u51E0\u4E2A\u756A\u8304\u8FDB\u5165\u4E00\u6B21\u957F\u4F11\u606F\uFF081\u201312\uFF09", "Long break after every N pomodoros (1\u201312)"),
      1,
      12,
      () => this.plugin.settings.pomodoro.cycles,
      async (v) => {
        this.plugin.settings.pomodoro.cycles = v;
        await this.plugin.saveSettings();
      }
    );
    new import_obsidian.Setting(containerEl).setName(this.s("\u81EA\u52A8\u5F00\u59CB\u4E0B\u4E00\u6BB5", "Auto-start next session")).setDesc(this.s("\u4E00\u6BB5\u7ED3\u675F\u540E\u81EA\u52A8\u5F00\u59CB\u4E0B\u4E00\u6BB5\uFF08\u4F11\u606F / \u4E13\u6CE8\uFF09", "Automatically start the next session (break / focus) when one ends")).addToggle(
      (t) => t.setValue(this.plugin.settings.pomodoro.autoStart).onChange(async (v) => {
        this.plugin.settings.pomodoro.autoStart = v;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.s("\u7ED3\u675F\u63D0\u793A\u97F3", "End sound")).setDesc(this.s("\u4E00\u6BB5\u7ED3\u675F\u65F6\u64AD\u653E\u63D0\u793A\u97F3", "Play a sound when a session ends")).addToggle(
      (t) => t.setValue(this.plugin.settings.pomodoro.sound).onChange(async (v) => {
        this.plugin.settings.pomodoro.sound = v;
        await this.plugin.saveSettings();
      })
    );
    const pomoResetRow = new import_obsidian.Setting(containerEl).setName(this.s("\u4ECA\u65E5\u756A\u8304\u6570", "Pomodoros today")).setDesc(this.s("\u4ECA\u65E5\u5DF2\u5B8C\u6210\u7684\u756A\u8304\u8BA1\u6570", "Pomodoros completed today"));
    pomoResetRow.addButton((b) => b.setButtonText(this.s("\u6E05\u96F6", "Reset")).onClick(async () => {
      this.plugin.settings.pomodoro.dailyCount = 0;
      this.plugin.settings.pomodoro.dailyDate = "";
      await this.plugin.saveSettings();
      this.plugin.refreshHome();
    }));
    new import_obsidian.Setting(containerEl).setName(this.s("\u5929\u6C14", "Weather")).setHeading();
    const weatherCities = ((_b = (_a = this.plugin.settings.weather) == null ? void 0 : _a.cities) == null ? void 0 : _b.length) ? this.plugin.settings.weather.cities : ["\u65E5\u7167"];
    [0, 1, 2].forEach((idx) => {
      new import_obsidian.Setting(containerEl).setName(idx === 0 ? this.s("\u57CE\u5E02 1", "City 1") : this.s(`\u57CE\u5E02 ${idx + 1}`, `City ${idx + 1}`)).setDesc(idx === 0 ? this.s("\u663E\u793A\u5B9E\u65F6\u5929\u6C14\u7684\u57CE\u5E02\uFF08\u6700\u591A 3 \u4E2A\uFF0Cwttr.in \u652F\u6301\u4E2D\u6587\u57CE\u5E02\u540D\uFF0C\u5982 \u65E5\u7167\uFF09\u3002\u7559\u7A7A\u5219\u4E0D\u663E\u793A\u8BE5\u57CE\u5E02", "City to show live weather (max 3, wttr.in supports Chinese city names). Leave empty to hide") : this.s("\u7B2C\u4E8C\u4E2A/\u7B2C\u4E09\u4E2A\u57CE\u5E02\u53EF\u9009\uFF0C\u7559\u7A7A\u5219\u4E0D\u663E\u793A", "Optional 2nd / 3rd city; leave empty to hide")).addText(
        (t) => {
          var _a2;
          return t.setPlaceholder(idx === 0 ? "\u65E5\u7167" : this.s("\uFF08\u53EF\u9009\uFF09", "(optional)")).setValue((_a2 = weatherCities[idx]) != null ? _a2 : "").onChange(async (v) => {
            const cur = [...weatherCities];
            if (v.trim()) cur[idx] = v.trim();
            else delete cur[idx];
            this.plugin.settings.weather = { cities: cur.filter(Boolean).slice(0, 3) };
            await this.plugin.saveSettings();
            this.plugin.refreshHome();
          });
        }
      );
    });
    new import_obsidian.Setting(containerEl).setName(this.s("\u5916\u89C2", "Appearance")).setHeading();
    new import_obsidian.Setting(containerEl).setName(this.s("\u4E3B\u9898", "Theme")).setDesc(this.s("\u8DDF\u968F Obsidian \u5916\u89C2\uFF0C\u6216\u624B\u52A8\u6307\u5B9A\u6DF1\u8272/\u6D45\u8272\u3002\u624B\u52A8\u9009\u62E9\u4F1A\u540C\u65F6\u5207\u6362 Obsidian \u6574\u4F53\u5916\u89C2\uFF0C\u4EEA\u8868\u76D8\u81EA\u52A8\u8DDF\u968F", "Follow Obsidian appearance, or force dark / light. Manual choice also switches Obsidian theme; dashboard follows automatically")).addDropdown((dropdown) => {
      dropdown.addOption("auto", this.s("\u8DDF\u968F Obsidian", "Follow Obsidian"));
      dropdown.addOption("dark", this.s("\u6DF1\u8272", "Dark"));
      dropdown.addOption("light", this.s("\u6D45\u8272", "Light"));
      dropdown.setValue(this.plugin.settings.theme);
      dropdown.onChange(async (v) => {
        const mode = v;
        if (mode !== "auto") {
          this.plugin.setObsidianTheme(mode);
          this.plugin.settings.theme = "auto";
          dropdown.setValue("auto");
        } else {
          this.plugin.settings.theme = "auto";
        }
        await this.plugin.saveSettings();
        this.applyTheme();
      });
    });
    new import_obsidian.Setting(containerEl).setName(this.s("\u63D2\u4EF6\u6807\u9898", "Dashboard Title")).setDesc(this.s("\u81EA\u5B9A\u4E49\u4EEA\u8868\u76D8\u4E3B\u6807\u9898\uFF08\u5373\u201CMY DASHBOARD\u201D\u90A3\u4E00\u884C\uFF09\u3002\u7559\u7A7A\u5219\u4F7F\u7528\u9ED8\u8BA4\u6807\u9898 \u201CMY DASHBOARD\u201D\uFF0C\u4FEE\u6539\u540E\u7ACB\u5373\u751F\u6548\uFF0C\u65E0\u9700\u91CD\u8F7D", "Custom title of the dashboard (the \u201CMY DASHBOARD\u201D line). Leave empty to use the default; applies immediately")).addText(
      (t) => t.setPlaceholder("MY DASHBOARD").setValue(this.plugin.settings.dashboardTitle).onChange(async (v) => {
        this.plugin.settings.dashboardTitle = v;
        await this.plugin.saveSettings();
        this.plugin.refreshDashboardTitle();
      })
    );
    new import_obsidian.Setting(containerEl).setName(this.s("\u8BED\u8A00", "Language")).setDesc(this.s("\u754C\u9762\u8BED\u8A00\uFF1A\u4E2D\u6587 / English\uFF08\u5207\u6362\u540E\u8BBE\u7F6E\u9762\u677F\u7ACB\u5373\u5237\u65B0\uFF09", "UI language: \u4E2D\u6587 / English (settings refresh immediately)")).addDropdown((dropdown) => {
      var _a2;
      dropdown.addOption("zh", "\u4E2D\u6587");
      dropdown.addOption("en", "English");
      dropdown.setValue((_a2 = this.plugin.settings.language) != null ? _a2 : "zh");
      dropdown.onChange(async (v) => {
        this.plugin.settings.language = v || "zh";
        await this.plugin.saveSettings();
        this.display();
      });
    });
  }
  applyTheme() {
    const t = this.plugin.settings.theme;
    const effective = t === "auto" ? document.body.classList.contains("theme-light") ? "light" : "dark" : t;
    this.app.workspace.getLeavesOfType("dashboard-view").forEach((leaf) => {
      var _a, _b;
      (_b = (_a = leaf.view) == null ? void 0 : _a.containerEl.querySelector(".dashboard-plugin")) == null ? void 0 : _b.setAttribute("data-theme", effective);
    });
    document.querySelectorAll(".dashboard-plugin").forEach((el) => el.setAttribute("data-theme", effective));
    this.plugin.refreshThemeButtons();
  }
};

// src/views/DashboardView.ts
var import_obsidian5 = require("obsidian");

// src/data/mockData.ts
var MOCK_DATA = {
  today: "2026-06-29",
  weekday: "\u661F\u671F\u4E00",
  lunar: "\u519C\u5386 \u4E94\u6708\u5341\u4E94",
  header: {
    eyebrow: "SECOND BRAIN",
    title: "MY DASHBOARD",
    subtitle: "Obsidian \xB7 Personal Dashboard \xB7 v0.2.3"
  },
  pulse: {
    notes: 156,
    pending: 23,
    delta_today: 4,
    streak_days: 12
  },
  quick_capture: {
    placeholder: "\u628A\u5FF5\u5934\u3001\u95EA\u5FF5\u6216\u94FE\u63A5\u4E22\u8FDB\u6765\u2026",
    primary_cta: "\u521B\u5EFA"
  },
  today_todos: [
    { id: "t1", priority: "p0", text: "\u63D0\u4EA4 GA \u9879\u76EE PRD v2 \u7ED9\u8BC4\u5BA1", done: false, tag: "GA" },
    { id: "t2", priority: "p1", text: "\u8865\u5168 Dashboard \u7684 ItemView \u9AA8\u67B6", done: false, tag: "dev" },
    { id: "t3", priority: "p1", text: "\u56DE 3 \u6761 async \u7559\u8A00\uFF08@bobo @lily @mark\uFF09", done: false, tag: "sync" },
    { id: "t4", priority: "p2", text: '\u6574\u7406 "weekly review" \u6A21\u677F', done: true, tag: "note" },
    { id: "t5", priority: "p2", text: "\u8BFB Diff Screenshot Service RFC", done: false, tag: "read" },
    { id: "t6", priority: "p3", text: "\u4E3A\u65B0\u7B14\u8BB0 archive/2026-06 \u5F52\u6863", done: false, tag: "chore" },
    { id: "t7", priority: "p3", text: "\u6E05\u7406 inbox \u91CC 5 \u6761\u4E34\u65F6\u6587\u4EF6", done: true, tag: "chore" },
    { id: "t8", priority: "p3", text: "Backup vault \u589E\u91CF\u6821\u9A8C", done: false, tag: "chore" }
  ],
  daily_progress: {
    completed: 5,
    total: 10,
    delta_vs_yesterday: "+2"
  },
  weekly_and_overdue: {
    overdue: [
      { id: "o1", date: "06-25", text: "\u5411 mentor \u63D0\u4EA4 Q2 \u590D\u76D8", owner: "@xw" },
      { id: "o2", date: "06-27", text: "\u4FEE Obsidian 0.15 \u517C\u5BB9\uFF1AWorkspaceLeaf.onload", owner: "@xw" }
    ],
    this_week: [
      { id: "w1", date: "06-29", text: "Dashboard \u9759\u6001\u539F\u578B\u9A8C\u6536", state: "today" },
      { id: "w2", date: "06-30", text: "GA \u7ACB\u9879\u4F1A \xB7 \u51C6\u5907 deck 23p", state: "soon" },
      { id: "w3", date: "07-01", text: "Notes pipeline \u91CD\u6784\u8BBE\u8BA1\u8BC4\u5BA1", state: "later" },
      { id: "w4", date: "07-02", text: "\u5199\u4E00\u7BC7\u5173\u4E8E vault-as-state \u7684\u535A\u5BA2\u8349\u7A3F", state: "later" },
      { id: "w5", date: "07-03", text: "\u5468\u4E94 weekly review\uFF0830min\uFF09", state: "recurring" },
      { id: "w6", date: "07-04", text: "\u6574\u7406\u8BFB\u4E66\u7B14\u8BB0\u300ADesigning Data-Intensive Apps\u300B", state: "later" }
    ]
  },
  projects: [
    { id: "p1", name: "Dashboard", owner: "@xw", type: "dev", stage: 2, stages: ["\u7ACB\u9879", "\u89C4\u5212", "\u5F00\u53D1", "\u6D4B\u8BD5", "\u4E0A\u7EBF"], percent: 42, next: "\u5B8C\u5584 ItemView \u9AA8\u67B6 & \u8BBE\u7F6E\u9762\u677F" },
    { id: "p2", name: "Diff Screenshot Service", owner: "@team", type: "dev", stage: 3, stages: ["\u7ACB\u9879", "\u89C4\u5212", "\u5F00\u53D1", "\u6D4B\u8BD5", "\u4E0A\u7EBF"], percent: 68, next: "\u6D4B\u8BD5\u7528\u4F8B\u8865\u5168 & \u6027\u80FD profile" },
    { id: "p3", name: "Q2 GA \u4E0A\u7EBF", owner: "@ops", type: "ga", stage: 1, stages: ["\u7ACB\u9879", "\u89C4\u5212", "\u5F00\u53D1", "\u6D4B\u8BD5", "\u4E0A\u7EBF"], percent: 18, next: "\u5BF9\u9F50 GTM \u65F6\u95F4\u7EBF" },
    { id: "p4", name: "Notes Pipeline v2", owner: "@xw", type: "dev", stage: 1, stages: ["\u7ACB\u9879", "\u89C4\u5212", "\u5F00\u53D1", "\u6D4B\u8BD5", "\u4E0A\u7EBF"], percent: 25, next: "\u7EC6\u5316 ingestion \u63A5\u53E3" },
    { id: "p5", name: "\u54C1\u724C\u8D44\u4EA7 GA", owner: "@design", type: "ga", stage: 4, stages: ["\u7ACB\u9879", "\u89C4\u5212", "\u5F00\u53D1", "\u6D4B\u8BD5", "\u4E0A\u7EBF"], percent: 90, next: "\u4E0A\u7EBF checklist \u6821\u9A8C" },
    { id: "p6", name: "Blog \u957F\u6587 \xB7 vault-as-state", owner: "@xw", type: "ga", stage: 0, stages: ["\u7ACB\u9879", "\u89C4\u5212", "\u5F00\u53D1", "\u6D4B\u8BD5", "\u4E0A\u7EBF"], percent: 8, next: "\u5B9A outline & \u5199\u5F00\u7BC7" }
  ],
  project_summary: { dev: 3, ga: 6 },
  notes_stats: {
    total: 200,
    active_days: 180,
    longest_streak_days: 41,
    current_streak_days: 12,
    year_label: "2026"
  },
  countdown: {
    year: 2026,
    days_left: 186,
    weeks_left: 27,
    percent_done: 49.2,
    milestone: "Q4 OKR \u542F\u52A8\u51C6\u5907"
  }
};

// src/views/FavoriteDocsModal.ts
var import_obsidian2 = require("obsidian");
var FavoriteDocsModal = class extends import_obsidian2.Modal {
  /** 按当前语言返回中 / 英文案 */
  t(zh, en) {
    var _a, _b, _c, _d;
    return ((_d = (_c = (_b = (_a = this.app.plugins) == null ? void 0 : _a.plugins) == null ? void 0 : _b["dashboard-private"]) == null ? void 0 : _c.settings) == null ? void 0 : _d.language) === "en" ? en : zh;
  }
  constructor(app, existingPaths, onConfirm) {
    super(app);
    this.files = app.vault.getFiles().filter((f) => f.extension === "md").sort((a, b) => a.path.localeCompare(b.path));
    this.existing = new Set(existingPaths);
    this.picked = /* @__PURE__ */ new Set();
    this.onConfirm = onConfirm;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.addClass("ad-modal");
    contentEl.createEl("h3", { cls: "ad-modal-title", text: this.t("\u6DFB\u52A0\u5E38\u7528\u6587\u6863", "Add Favorites") });
    this.searchEl = contentEl.createEl("input", {
      cls: "ad-modal-input ad-fav-search",
      type: "text",
      attr: { placeholder: this.t("\u641C\u7D22\u6587\u4EF6\u540D\u6216\u8DEF\u5F84\u2026", "Search file name or path\u2026") }
    });
    this.searchEl.addEventListener("input", () => this.renderList());
    this.listEl = contentEl.createDiv({ cls: "ad-fav-list" });
    contentEl.createDiv({ cls: "ad-modal-hint", text: this.t("\u52FE\u9009\u4E00\u4E2A\u6216\u591A\u4E2A\u6587\u6863\uFF0C\u70B9\u51FB\u300C\u6DFB\u52A0\u300D\u5373\u53EF\u56FA\u5B9A\u5230\u9996\u9875\u5E38\u7528\u6587\u6863\u5361\u7247\u3002", "Check one or more files and click Add to pin them to the home Favorites card.") });
    const btns = contentEl.createDiv({ cls: "ad-modal-btns" });
    const cancelBtn = btns.createEl("button", { cls: "ad-modal-btn", text: this.t("\u53D6\u6D88", "Cancel") });
    const addBtn = btns.createEl("button", { cls: "ad-modal-btn ad-modal-btn--primary", text: this.t("\u6DFB\u52A0", "Add") });
    cancelBtn.addEventListener("click", () => this.close());
    addBtn.addEventListener("click", () => {
      const docs = this.files.filter((f) => this.picked.has(f.path)).map((f) => ({ path: f.path, title: f.basename }));
      if (docs.length) this.onConfirm(docs);
      this.close();
    });
    this.renderList();
    this.searchEl.focus();
  }
  renderList() {
    const q = this.searchEl.value.trim().toLowerCase();
    this.listEl.empty();
    for (const f of this.files) {
      if (q && !f.path.toLowerCase().includes(q) && !f.basename.toLowerCase().includes(q)) continue;
      const already = this.existing.has(f.path);
      const row = this.listEl.createDiv({ cls: "ad-fav-item" + (already ? " is-added" : "") });
      const cb = row.createEl("input", { type: "checkbox" });
      cb.checked = already || this.picked.has(f.path);
      cb.disabled = already;
      cb.addEventListener("change", () => {
        if (cb.checked) this.picked.add(f.path);
        else this.picked.delete(f.path);
      });
      row.createSpan({ cls: "ad-fav-name", text: f.basename });
      const parent = f.parent && f.parent.path !== "/" ? f.parent.path : "/";
      row.createSpan({ cls: "ad-fav-path", text: parent });
      if (already) row.createSpan({ cls: "ad-fav-tag", text: this.t("\u5DF2\u6DFB\u52A0", "Added") });
    }
  }
  onClose() {
    this.contentEl.empty();
  }
};

// src/views/KanbanEditModal.ts
var import_obsidian3 = require("obsidian");
var KanbanEditModal = class extends import_obsidian3.Modal {
  constructor(app, current, onConfirm) {
    super(app);
    this.current = current;
    this.onConfirm = onConfirm;
  }
  /** 按当前语言返回中 / 英文案 */
  t(zh, en) {
    var _a, _b, _c, _d;
    return ((_d = (_c = (_b = (_a = this.app.plugins) == null ? void 0 : _a.plugins) == null ? void 0 : _b["dashboard-private"]) == null ? void 0 : _c.settings) == null ? void 0 : _d.language) === "en" ? en : zh;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.addClass("ad-modal");
    contentEl.createEl("h3", { cls: "ad-modal-title", text: this.t("\u7F16\u8F91\u5F85\u529E", "Edit To-do") });
    const input = contentEl.createEl("input", {
      cls: "ad-modal-input",
      type: "text",
      value: this.current,
      attr: { placeholder: this.t("\u5F85\u529E\u5185\u5BB9\u2026", "To-do content\u2026") }
    });
    input.select();
    const btns = contentEl.createDiv({ cls: "ad-modal-btns" });
    const cancelBtn = btns.createEl("button", { cls: "ad-modal-btn", text: this.t("\u53D6\u6D88", "Cancel") });
    const saveBtn = btns.createEl("button", { cls: "ad-modal-btn ad-modal-btn--primary", text: this.t("\u4FDD\u5B58", "Save") });
    const submit = () => {
      const text = input.value.trim();
      if (!text) return;
      this.onConfirm(text);
      this.close();
    };
    cancelBtn.addEventListener("click", () => this.close());
    saveBtn.addEventListener("click", submit);
    input.addEventListener("keydown", (e) => {
      if (e.key === "Enter") submit();
    });
  }
  onClose() {
    this.contentEl.empty();
  }
};

// src/views/SiteEditModal.ts
var import_obsidian4 = require("obsidian");
var SiteEditModal = class extends import_obsidian4.Modal {
  /** 按当前语言返回中 / 英文案 */
  t(zh, en) {
    var _a, _b, _c, _d;
    return ((_d = (_c = (_b = (_a = this.app.plugins) == null ? void 0 : _a.plugins) == null ? void 0 : _b["dashboard-private"]) == null ? void 0 : _c.settings) == null ? void 0 : _d.language) === "en" ? en : zh;
  }
  constructor(app, initial, onConfirm) {
    var _a, _b, _c, _d;
    super(app);
    this.onConfirm = onConfirm;
    this.form = {
      name: (_a = initial.name) != null ? _a : "",
      url: (_b = initial.url) != null ? _b : "",
      category: (_c = initial.category) != null ? _c : "",
      keywords: (_d = initial.keywords) != null ? _d : ""
    };
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.addClass("ad-modal");
    contentEl.createEl("h3", { cls: "ad-modal-title", text: this.form.name ? this.t("\u7F16\u8F91\u7AD9\u70B9", "Edit Site") : this.t("\u6DFB\u52A0\u7AD9\u70B9", "Add Site") });
    const fields = [
      ["name", this.t("\u7AD9\u70B9\u540D\u79F0", "Site name"), this.t("\u5982 \u817E\u8BAF\u4E91\uFF08\u7559\u7A7A\u5219\u663E\u793A\u94FE\u63A5\uFF09", "e.g. Tencent Cloud (empty shows the link)")],
      ["url", this.t("\u7F51\u5740 URL", "URL"), "https://\u2026"],
      ["category", this.t("\u5206\u7C7B", "Category"), this.t("\u5982 \u5DE5\u5177 / \u5DE5\u4F5C / \u8D44\u8BAF\uFF08\u7559\u7A7A\u5F52\u300C\u672A\u5206\u7C7B\u300D\uFF09", "e.g. Tools / Work / News (empty = Uncategorized)")],
      ["keywords", this.t("\u5173\u952E\u8BCD", "Keywords"), this.t("\u9017\u53F7\u5206\u9694\uFF0C\u7528\u4E8E\u641C\u7D22\uFF0C\u5982 \u4E91, \u670D\u52A1\u5668", "Comma separated, for search, e.g. cloud, server")]
    ];
    const inputs = {};
    for (const [key, label, ph] of fields) {
      const field = contentEl.createDiv({ cls: "ad-modal-field" });
      field.createEl("label", { cls: "ad-modal-label", text: label });
      const input = field.createEl("input", { cls: "ad-modal-input", type: "text", value: this.form[key] });
      input.placeholder = ph;
      inputs[key] = input;
    }
    const btns = contentEl.createDiv({ cls: "ad-modal-btns" });
    const cancelBtn = btns.createEl("button", { cls: "ad-modal-btn", text: this.t("\u53D6\u6D88", "Cancel") });
    const saveBtn = btns.createEl("button", { cls: "ad-modal-btn ad-modal-btn--primary", text: this.t("\u4FDD\u5B58", "Save") });
    const submit = () => {
      const rawUrl = inputs["url"].value.trim();
      if (!rawUrl) return;
      this.onConfirm({
        name: inputs["name"].value.trim(),
        url: /^https?:\/\//i.test(rawUrl) ? rawUrl : "https://" + rawUrl,
        category: inputs["category"].value.trim(),
        keywords: inputs["keywords"].value.trim()
      });
      this.close();
    };
    cancelBtn.addEventListener("click", () => this.close());
    saveBtn.addEventListener("click", submit);
    inputs["url"].addEventListener("keydown", (e) => {
      if (e.key === "Enter") submit();
    });
    inputs["name"].focus();
  }
  onClose() {
    this.contentEl.empty();
  }
};

// src/views/touchMenu.ts
function attachTouchMenu(el, onMenu) {
  if (!window.matchMedia("(hover: none)").matches) return;
  let timer = null;
  let sx = 0;
  let sy = 0;
  const cancel = () => {
    if (timer !== null) {
      window.clearTimeout(timer);
      timer = null;
    }
  };
  el.addEventListener("pointerdown", (e) => {
    if (e.pointerType !== "touch") return;
    sx = e.clientX;
    sy = e.clientY;
    cancel();
    timer = window.setTimeout(() => {
      timer = null;
      const block = (ev) => {
        ev.stopPropagation();
        ev.preventDefault();
      };
      el.addEventListener("click", block, { capture: true, once: true });
      onMenu(e);
    }, 500);
  });
  el.addEventListener("pointermove", (e) => {
    if (timer !== null && (Math.abs(e.clientX - sx) > 10 || Math.abs(e.clientY - sy) > 10)) cancel();
  });
  el.addEventListener("pointerup", cancel);
  el.addEventListener("pointercancel", cancel);
  el.addEventListener("pointerleave", cancel);
}

// src/data/kanbanParser.ts
function stripFrontmatterLines(lines) {
  var _a, _b, _c, _d, _e;
  const fm = {};
  if (((_a = lines[0]) == null ? void 0 : _a.trim()) !== "---") return { fm, bodyStart: 0 };
  let i = 1;
  for (; i < lines.length; i++) {
    if (((_b = lines[i]) == null ? void 0 : _b.trim()) === "---") break;
    const m = /^([^:]+):\s*(.*)$/.exec((_c = lines[i]) != null ? _c : "");
    if (m) fm[((_d = m[1]) != null ? _d : "").trim()] = ((_e = m[2]) != null ? _e : "").trim();
  }
  return { fm, bodyStart: i < lines.length ? i + 1 : lines.length };
}
function splitTaskRest(rest) {
  var _a, _b, _c;
  let content = rest;
  let id = "";
  let status = "";
  let doneDate = null;
  const idM = /\[ID::\s*([^\]]+)\]/.exec(content);
  if (idM) {
    id = ((_a = idM[1]) != null ? _a : "").trim();
    content = content.replace(idM[0], " ");
  }
  const stM = /\[状态::\s*([^\]]+)\]/.exec(content);
  if (stM) {
    status = ((_b = stM[1]) != null ? _b : "").trim();
    content = content.replace(stM[0], " ");
  }
  const ddM = /✅\s*(\d{4}-\d{2}-\d{2})/.exec(content);
  if (ddM) {
    doneDate = (_c = ddM[1]) != null ? _c : null;
    content = content.replace(ddM[0], " ");
  }
  content = content.replace(/✅/g, " ").replace(/\s+/g, " ").trim();
  return { content, id, status, doneDate };
}
function taskMark(line) {
  const m = /^\s*-\s*\[([ xX])\]\s*/.exec(line);
  if (!m) return null;
  return m[1] === " " ? "todo" : "done";
}
function parseKanbanBoard(content) {
  const rawLines = content.split(/\r?\n/);
  const { fm } = stripFrontmatterLines(rawLines);
  const isKanban = "kanban-plugin" in fm;
  const columns = [];
  const tasks = [];
  let current = null;
  rawLines.forEach((line, i) => {
    var _a;
    const h = /^##\s+(.+?)\s*$/.exec(line.trim());
    if (h) {
      current = { title: ((_a = h[1]) != null ? _a : "").trim(), tasks: [] };
      columns.push(current);
      return;
    }
    const mark = taskMark(line);
    if (mark && current) {
      const rest = line.replace(/^\s*-\s*\[[ xX]\]\s*/, "");
      const { content: body, id, status, doneDate } = splitTaskRest(rest);
      const task = {
        id: id || `line-${i}`,
        content: body || rest.trim(),
        done: mark === "done",
        status: status || (mark === "done" ? "\u5DF2\u5B8C\u6210" : "\u5F85\u529E"),
        doneDate,
        column: current.title,
        line: i
      };
      current.tasks.push(task);
      tasks.push(task);
    }
  });
  return { isKanban, columns, tasks, rawLines };
}
function joinLines(lines) {
  return lines.join("\n");
}
function taskLocator(line) {
  const mark = taskMark(line);
  if (!mark) return null;
  const rest = line.replace(/^\s*-\s*\[[ xX]\]\s*/, "");
  const { content } = splitTaskRest(rest);
  return { content, done: mark === "done" };
}
function locateTaskLine(lines, task) {
  var _a, _b;
  if (task.line >= 0 && task.line < lines.length) {
    const t = taskLocator((_a = lines[task.line]) != null ? _a : "");
    if (t && t.content === task.content && t.done === task.done) return task.line;
  }
  for (let i = 0; i < lines.length; i++) {
    const t = taskLocator((_b = lines[i]) != null ? _b : "");
    if (t && t.content === task.content && t.done === task.done) return i;
  }
  return -1;
}
function toggleKanbanTask(content, task, done, today) {
  var _a;
  const lines = content.split(/\r?\n/);
  const idx = locateTaskLine(lines, task);
  if (idx < 0) return null;
  let nl = (_a = lines[idx]) != null ? _a : "";
  nl = nl.replace(/^\s*-\s*\[[ xX]\]/, done ? "- [x]" : "- [ ]");
  if (/\[状态::\s*[^\]]+\]/.test(nl)) {
    nl = nl.replace(/\[状态::\s*[^\]]+\]/, `[\u72B6\u6001:: ${done ? "\u5DF2\u5B8C\u6210" : "\u5F85\u529E"}]`);
  }
  if (done) {
    if (!/✅/.test(nl)) nl = `${nl} \u2705 ${today}`;
    else nl = nl.replace(/✅\s*\d{4}-\d{2}-\d{2}/, `\u2705 ${today}`);
  } else {
    nl = nl.replace(/\s*✅\s*\d{4}-\d{2}-\d{2}/g, "").replace(/\s*✅/g, "");
  }
  lines[idx] = nl;
  return joinLines(lines);
}
function addKanbanTask(content, columnTitle, text) {
  var _a, _b, _c, _d, _e;
  const lines = content.split(/\r?\n/);
  const newLine = `- [ ] ${text.trim()}`;
  let headingIdx = -1;
  for (let i2 = 0; i2 < lines.length; i2++) {
    const h = /^##\s+(.+?)\s*$/.exec(((_a = lines[i2]) != null ? _a : "").trim());
    if (h && ((_b = h[1]) != null ? _b : "").trim() === columnTitle) {
      headingIdx = i2;
      break;
    }
  }
  if (headingIdx === -1) return null;
  let i = headingIdx + 1;
  while (i < lines.length && ((_c = lines[i]) != null ? _c : "").trim() === "") i++;
  if (i < lines.length && /^\s*-\s/.test((_d = lines[i]) != null ? _d : "")) {
    lines.splice(i, 0, newLine);
  } else {
    if (((_e = lines[i - 1]) != null ? _e : "").trim() !== "") lines.splice(i, 0, "");
    lines.splice(i, 0, newLine);
  }
  return joinLines(lines);
}
function removeKanbanTask(content, task) {
  const lines = content.split(/\r?\n/);
  const idx = locateTaskLine(lines, task);
  if (idx < 0) return null;
  lines.splice(idx, 1);
  return joinLines(lines);
}
function editKanbanTask(content, task, newText) {
  var _a;
  const lines = content.split(/\r?\n/);
  const text = newText.trim();
  if (!text) return null;
  const idx = locateTaskLine(lines, task);
  if (idx < 0) return null;
  const line = (_a = lines[idx]) != null ? _a : "";
  const mark = taskMark(line);
  if (!mark) return null;
  const idM = /\[ID::\s*[^\]]+\]/.exec(line);
  const stM = /\[状态::\s*[^\]]+\]/.exec(line);
  const ddM = /✅\s*\d{4}-\d{2}-\d{2}/.exec(line);
  const tail = [idM ? idM[0] : "", stM ? stM[0] : "", ddM ? ddM[0] : ""].filter(Boolean).join(" ");
  lines[idx] = `${mark === "done" ? "- [x]" : "- [ ]"} ${text}${tail ? " " + tail : ""}`.trim();
  return joinLines(lines);
}

// src/data/sitesParser.ts
function parseSiteLine(line) {
  var _a, _b, _c;
  const m = /^\s*-\s*(?:\[[^\]]+\]\s*)?(.*?)\s*→\s*(\S+)(?:\s+(.*))?$/.exec(line.trim());
  if (!m) return null;
  const name = ((_a = m[1]) != null ? _a : "").trim();
  const url = ((_b = m[2]) != null ? _b : "").trim();
  if (!url) return null;
  const tail = ((_c = m[3]) != null ? _c : "").trim();
  const tags = [...tail.matchAll(/#([^\s#]+)/g)].map((x) => {
    var _a2;
    return ((_a2 = x[1]) != null ? _a2 : "").trim();
  }).filter(Boolean);
  const site = { name, url };
  if (tags.length) site.keywords = tags.join(" ");
  return site;
}
function parseSitesMd(content) {
  const lines = content.split(/\r?\n/);
  const out = [];
  let cat = "";
  lines.forEach((line, i) => {
    var _a;
    const h = /^##\s+(.+?)\s*$/.exec(line.trim());
    if (h) {
      cat = ((_a = h[1]) != null ? _a : "").trim();
      return;
    }
    const site = parseSiteLine(line);
    if (site) {
      if (cat) site.category = cat;
      out.push({ site, line: i });
    }
  });
  return out;
}
function siteToLine(s) {
  const parts = [];
  if (s.name && s.name.trim()) parts.push(s.name.trim());
  parts.push("\u2192", s.url);
  if (s.keywords && s.keywords.trim()) {
    parts.push(s.keywords.trim().split(/\s+/).filter(Boolean).map((k) => `#${k}`).join(" "));
  }
  return "- " + parts.join(" ");
}
function serializeSites(sites) {
  const cats = /* @__PURE__ */ new Map();
  for (const s of sites) {
    const c = s.category && s.category.trim() ? s.category.trim() : "";
    if (!cats.has(c)) cats.set(c, []);
    cats.get(c).push(s);
  }
  const parts = ["# \u7F51\u7AD9\u6536\u85CF", ""];
  for (const [cat, list] of cats) {
    if (cat) parts.push(`## ${cat}`, "");
    for (const s of list) parts.push(siteToLine(s));
    parts.push("");
  }
  return parts.join("\n").replace(/\n+$/, "\n");
}
function findUrlLine(lines, url) {
  var _a;
  for (let i = 0; i < lines.length; i++) {
    const s = parseSiteLine((_a = lines[i]) != null ? _a : "");
    if (s && s.url === url) return i;
  }
  return -1;
}
function categoryOfLine(lines, idx) {
  var _a, _b;
  for (let i = idx - 1; i >= 0; i--) {
    const h = /^##\s+(.+?)\s*$/.exec(((_a = lines[i]) != null ? _a : "").trim());
    if (h) return ((_b = h[1]) != null ? _b : "").trim();
  }
  return "";
}
function insertIntoCategory(lines, cat, line) {
  var _a, _b, _c, _d, _e, _f, _g;
  if (cat) {
    let headIdx = -1;
    for (let i = 0; i < lines.length; i++) {
      const h = /^##\s+(.+?)\s*$/.exec(((_a = lines[i]) != null ? _a : "").trim());
      if (h && ((_b = h[1]) != null ? _b : "").trim() === cat) {
        headIdx = i;
        break;
      }
    }
    if (headIdx >= 0) {
      let end = lines.length;
      for (let i = headIdx + 1; i < lines.length; i++) {
        if (/^##\s+/.test(((_c = lines[i]) != null ? _c : "").trim())) {
          end = i;
          break;
        }
      }
      let ins = end;
      while (ins - 1 > headIdx && ((_d = lines[ins - 1]) != null ? _d : "").trim() === "") ins--;
      lines.splice(ins, 0, line);
      return lines;
    }
    if (((_e = lines[lines.length - 1]) == null ? void 0 : _e.trim()) !== "") lines.push("");
    lines.push(`## ${cat}`, "", line, "");
    return lines;
  }
  let firstHead = -1;
  for (let i = 0; i < lines.length; i++) {
    if (/^##\s+/.test(((_f = lines[i]) != null ? _f : "").trim())) {
      firstHead = i;
      break;
    }
  }
  if (firstHead < 0) {
    if (((_g = lines[lines.length - 1]) == null ? void 0 : _g.trim()) !== "") lines.push("");
    lines.push(line, "");
  } else {
    lines.splice(firstHead, 0, line);
  }
  return lines;
}
function upsertSiteLine(content, site) {
  const lines = content.split(/\r?\n/);
  const cat = site.category && site.category.trim() ? site.category.trim() : "";
  const line = siteToLine(site);
  const existIdx = findUrlLine(lines, site.url);
  if (existIdx >= 0) {
    if (categoryOfLine(lines, existIdx) === cat) {
      lines[existIdx] = line;
    } else {
      lines.splice(existIdx, 1);
      insertIntoCategory(lines, cat, line);
    }
    return lines.join("\n");
  }
  insertIntoCategory(lines, cat, line);
  return lines.join("\n");
}
function removeSiteLine(content, url) {
  const lines = content.split(/\r?\n/);
  const idx = findUrlLine(lines, url);
  if (idx < 0) return null;
  lines.splice(idx, 1);
  return lines.join("\n");
}
function moveSiteLine(content, url, delta) {
  var _a, _b;
  const lines = content.split(/\r?\n/);
  const idx = findUrlLine(lines, url);
  if (idx < 0) return null;
  const cat = (() => {
    var _a2, _b2;
    for (let i = idx - 1; i >= 0; i--) {
      const h = /^##\s+(.+?)\s*$/.exec(((_a2 = lines[i]) != null ? _a2 : "").trim());
      if (h) return ((_b2 = h[1]) != null ? _b2 : "").trim();
    }
    return "";
  })();
  const members = [];
  for (let i = 0; i < lines.length; i++) {
    const isHeading = /^##\s+/.test(((_a = lines[i]) != null ? _a : "").trim());
    const curCat = (() => {
      var _a2, _b2;
      for (let j = i - 1; j >= 0; j--) {
        const h = /^##\s+(.+?)\s*$/.exec(((_a2 = lines[j]) != null ? _a2 : "").trim());
        if (h) return ((_b2 = h[1]) != null ? _b2 : "").trim();
      }
      return "";
    })();
    if (!isHeading && curCat === cat && parseSiteLine((_b = lines[i]) != null ? _b : "")) members.push(i);
  }
  const pos = members.indexOf(idx);
  if (pos < 0) return null;
  const target = pos + delta;
  if (target < 0 || target >= members.length) return null;
  const a = members[pos], b = members[target];
  [lines[a], lines[b]] = [lines[b], lines[a]];
  return lines.join("\n");
}

// src/data/favDocsParser.ts
function parseDocLine(line) {
  var _a, _b;
  const m = /^\s*-\s*\[\[([^\]|]+?)(?:\|([^\]]+))?\]\]\s*$/.exec(line.trim());
  if (!m) return null;
  let path = ((_a = m[1]) != null ? _a : "").trim();
  if (!path) return null;
  if (!/\.md$/i.test(path)) path += ".md";
  const title = ((_b = m[2]) != null ? _b : "").trim();
  return { path, title };
}
function parseDocsMd(content) {
  const lines = content.split(/\r?\n/);
  const out = [];
  lines.forEach((line, i) => {
    const doc = parseDocLine(line);
    if (doc) out.push({ doc, line: i });
  });
  return out;
}
function docToLine(doc) {
  var _a;
  const path = doc.path.replace(/\.md$/i, "");
  const title = ((_a = doc.title) != null ? _a : "").trim();
  return title ? `- [[${path}|${title}]]` : `- [[${path}]]`;
}
function serializeDocs(docs) {
  const parts = ["# \u5E38\u7528\u6587\u6863", ""];
  for (const d of docs) parts.push(docToLine(d));
  return parts.join("\n") + "\n";
}
function findPathLine(lines, path) {
  var _a;
  for (let i = 0; i < lines.length; i++) {
    const d = parseDocLine((_a = lines[i]) != null ? _a : "");
    if (d && d.path === path) return i;
  }
  return -1;
}
function upsertDocLine(content, doc) {
  var _a, _b;
  const lines = content.split(/\r?\n/);
  const existIdx = findPathLine(lines, doc.path);
  const line = docToLine(doc);
  if (existIdx >= 0) {
    lines[existIdx] = line;
    return lines.join("\n");
  }
  let last = -1;
  for (let i = 0; i < lines.length; i++) {
    if (parseDocLine((_a = lines[i]) != null ? _a : "")) last = i;
  }
  if (last >= 0) {
    lines.splice(last + 1, 0, line);
  } else {
    const tail = ((_b = lines[lines.length - 1]) == null ? void 0 : _b.trim()) === "" ? lines : [...lines, ""];
    tail.push(line);
    return tail.join("\n") + "\n";
  }
  return lines.join("\n");
}
function removeDocLine(content, path) {
  const lines = content.split(/\r?\n/);
  const idx = findPathLine(lines, path);
  if (idx < 0) return null;
  lines.splice(idx, 1);
  return lines.join("\n");
}
function moveDocLine(content, path, delta) {
  var _a, _b;
  const lines = content.split(/\r?\n/);
  const idx = findPathLine(lines, path);
  if (idx < 0) return null;
  let target = -1;
  if (delta < 0) {
    for (let i = idx - 1; i >= 0; i--) {
      if (parseDocLine((_a = lines[i]) != null ? _a : "")) {
        target = i;
        break;
      }
    }
  } else {
    for (let i = idx + 1; i < lines.length; i++) {
      if (parseDocLine((_b = lines[i]) != null ? _b : "")) {
        target = i;
        break;
      }
    }
  }
  if (target < 0) return null;
  [lines[idx], lines[target]] = [lines[target], lines[idx]];
  return lines.join("\n");
}

// src/data/dateUtils.ts
function fmtDate(d) {
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, "0")}-${String(d.getDate()).padStart(2, "0")}`;
}
function todayStr(today = /* @__PURE__ */ new Date()) {
  return fmtDate(today);
}
function nowFmt(today = /* @__PURE__ */ new Date()) {
  const p = (n) => String(n).padStart(2, "0");
  return `${today.getFullYear()}-${p(today.getMonth() + 1)}-${p(today.getDate())} ${p(today.getHours())}:${p(today.getMinutes())}`;
}

// src/icons.ts
function injectSvg(el, svg) {
  const doc = new DOMParser().parseFromString(svg, "image/svg+xml");
  const node = doc.documentElement;
  if (node) el.replaceChildren(node);
}
var ICON_home = `<svg viewBox="0 0 80 80" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" width="80" height="80"><path fill="currentColor" transform="matrix(1 0 0 1 10.6117 25.2359)" d="M46.2906 47.1476L12.6422 47.1476C9.2781 47.1477 6.107 45.8289 3.7133 43.4344C1.3195 41.0398 0 37.8688 0 34.5055L0 2.8516C0 1.2766 1.2766 0 2.8516 0C4.4266 0 5.7031 1.2766 5.7031 2.8516L5.7031 34.5055C5.7031 38.3313 8.8156 41.4445 12.6422 41.4445L46.2914 41.4445C50.1172 41.4445 53.2305 38.332 53.2305 34.5055L53.2305 2.8516C53.2305 1.2766 54.507 0 56.082 0C57.657 0 58.9336 1.2766 58.9336 2.8516L58.9336 34.5055C58.9336 37.8695 57.6148 41.0406 55.2203 43.4344C52.8258 45.8281 49.6547 47.1477 46.2906 47.1476Z"/><path class="ad-ico-accent" transform="matrix(1 0 0 1 29.7031 35.3625)" d="M10.375 20.75C4.6539 20.75 0 16.0961 0 10.375C0 4.6539 4.6539 0 10.375 0C16.0961 0 20.75 4.6539 20.75 10.375C20.75 16.0961 16.0961 20.75 10.375 20.75ZM10.375 5.7031C7.7992 5.7031 5.7031 7.7992 5.7031 10.375C5.7031 12.9508 7.7992 15.0469 10.375 15.0469C12.9508 15.0469 15.0469 12.9508 15.0469 10.375C15.0469 7.7992 12.9508 5.7031 10.375 5.7031Z"/><path fill="currentColor" transform="matrix(1 0 0 1 5.55537 5.79365)" d="M2.8548 26.0899C1.9173 26.0899 0.9993 25.6282 0.454 24.7813C-0.3983 23.4571 -0.0155 21.6923 1.3087 20.8399L32.979 0.454C34.3032 -0.3983 36.0681 -0.0155 36.9204 1.3087C37.7728 2.6329 37.3899 4.3978 36.0657 5.2501L4.3954 25.6353C4.1661 25.7837 3.9204 25.8965 3.6583 25.9739C3.3961 26.0513 3.1283 26.0899 2.8548 26.0899Z"/><path fill="currentColor" transform="matrix(1 0 0 1 37.2257 5.79287)" d="M34.5204 26.0907C34.2472 26.0908 33.9795 26.0522 33.7173 25.975C33.4552 25.8978 33.2094 25.785 32.9798 25.6368L1.3087 5.2501C-0.0155 4.3978 -0.3983 2.6329 0.454 1.3087C1.3064 -0.0155 3.0712 -0.3983 4.3954 0.454L36.0665 20.8399C37.3907 21.6923 37.7735 23.4571 36.9212 24.7813C36.3759 25.629 35.4579 26.0907 34.5204 26.0907Z"/></svg>`;

// src/views/DashboardView.ts
var VIEW_TYPE = "dashboard-view";
var WEATHER_CODE = {
  "113": { icon: "\u2600", zh: "\u6674" },
  "116": { icon: "\u2601", zh: "\u591A\u4E91" },
  "119": { icon: "\u2601", zh: "\u9634" },
  "122": { icon: "\u2601", zh: "\u9634" },
  "143": { icon: "\u2261", zh: "\u96FE" },
  "248": { icon: "\u2261", zh: "\u96FE" },
  "260": { icon: "\u2261", zh: "\u96FE" },
  "176": { icon: "\u2602", zh: "\u9635\u96E8" },
  "263": { icon: "\u2602", zh: "\u5C0F\u96E8" },
  "266": { icon: "\u2602", zh: "\u5C0F\u96E8" },
  "293": { icon: "\u2602", zh: "\u9635\u96E8" },
  "296": { icon: "\u2602", zh: "\u5C0F\u96E8" },
  "299": { icon: "\u2602", zh: "\u4E2D\u96E8" },
  "302": { icon: "\u2602", zh: "\u4E2D\u96E8" },
  "305": { icon: "\u2602", zh: "\u5927\u96E8" },
  "308": { icon: "\u2602", zh: "\u5927\u96E8" },
  "353": { icon: "\u2602", zh: "\u9635\u96E8" },
  "356": { icon: "\u2602", zh: "\u4E2D\u96E8" },
  "359": { icon: "\u2602", zh: "\u5927\u96E8" },
  "386": { icon: "\u26A1", zh: "\u96F7\u9635\u96E8" },
  "389": { icon: "\u26A1", zh: "\u96F7\u9635\u96E8" },
  "200": { icon: "\u26A1", zh: "\u96F7\u9635\u96E8" },
  "227": { icon: "\u2746", zh: "\u5C0F\u96EA" },
  "230": { icon: "\u2746", zh: "\u4E2D\u96EA" },
  "320": { icon: "\u2746", zh: "\u9635\u96EA" },
  "323": { icon: "\u2746", zh: "\u5C0F\u96EA" },
  "326": { icon: "\u2746", zh: "\u5C0F\u96EA" },
  "329": { icon: "\u2746", zh: "\u4E2D\u96EA" },
  "332": { icon: "\u2746", zh: "\u4E2D\u96EA" },
  "335": { icon: "\u2746", zh: "\u5927\u96EA" },
  "338": { icon: "\u2746", zh: "\u5927\u96EA" },
  "368": { icon: "\u2746", zh: "\u9635\u96EA" },
  "371": { icon: "\u2746", zh: "\u5927\u96EA" },
  "392": { icon: "\u26A1", zh: "\u96F7\u96EA" },
  "395": { icon: "\u2746", zh: "\u5927\u96EA" },
  "179": { icon: "\u2746", zh: "\u96E8\u5939\u96EA" },
  "182": { icon: "\u2746", zh: "\u96E8\u5939\u96EA" },
  "185": { icon: "\u2746", zh: "\u96E8\u5939\u96EA" },
  "281": { icon: "\u2261", zh: "\u51BB\u96E8" },
  "284": { icon: "\u2261", zh: "\u51BB\u96E8" },
  "311": { icon: "\u2602", zh: "\u51BB\u96E8" },
  "314": { icon: "\u2602", zh: "\u51BB\u96E8" },
  "350": { icon: "\u2746", zh: "\u51B0\u96F9" },
  "374": { icon: "\u2746", zh: "\u51B0\u96F9" },
  "377": { icon: "\u2746", zh: "\u51B0\u96F9" }
};
var WEATHER_EN = {
  "\u6674": "Sunny",
  "\u591A\u4E91": "Cloudy",
  "\u9634": "Overcast",
  "\u96FE": "Fog",
  "\u9635\u96E8": "Showers",
  "\u5C0F\u96E8": "Light rain",
  "\u4E2D\u96E8": "Moderate rain",
  "\u5927\u96E8": "Heavy rain",
  "\u96F7\u9635\u96E8": "Thunderstorm",
  "\u96F7\u96EA": "Thunder snow",
  "\u96E8\u5939\u96EA": "Sleet",
  "\u51BB\u96E8": "Freezing rain",
  "\u51B0\u96F9": "Hail",
  "\u5C0F\u96EA": "Light snow",
  "\u4E2D\u96EA": "Moderate snow",
  "\u5927\u96EA": "Heavy snow",
  "\u9635\u96EA": "Snow showers"
};
var MAX_SPAN = 4;
var MIN_COLS = {
  "projects": 2,
  // 项目情况：最低宽度 2 格
  "heatmap": 2
  // 笔记统计：最低宽度 2 格（2×1 走窄版间距 + 自适应窗口）
};
var MIN_RATIO = {
  "projects": 2,
  // 项目情况：最低 2:1
  "heatmap": 3
  // 笔记统计：最低 3:1
};
function clampSpan(v) {
  const n = typeof v === "number" ? Math.round(v) : parseInt(String(v != null ? v : ""), 10);
  if (!Number.isFinite(n) || n < 1) return 1;
  return Math.min(MAX_SPAN, n);
}
function calcHeatmapStats(data, year, today) {
  var _a2;
  let total = 0;
  let active = 0;
  const prefix = `${year}-`;
  const todayStr4 = fmtDate(today);
  for (const [date, count] of data) {
    if (!date.startsWith(prefix) || date > todayStr4) continue;
    total += count;
    if (count > 0) active++;
  }
  let streak = 0;
  const d = new Date(today);
  while (d.getFullYear() === year) {
    const key = fmtDate(d);
    if (((_a2 = data.get(key)) != null ? _a2 : 0) > 0) streak++;
    else break;
    d.setDate(d.getDate() - 1);
  }
  return { total, active, streak };
}
function getLunarDate(d) {
  var _a2, _b, _c, _d, _e, _f;
  try {
    const parts = new Intl.DateTimeFormat("zh-CN-u-ca-chinese", {
      timeZone: "Asia/Shanghai",
      month: "long",
      day: "numeric"
    }).formatToParts(d);
    const monthStr = (_b = (_a2 = parts.find((p) => p.type === "month")) == null ? void 0 : _a2.value) != null ? _b : "";
    const dayStr = (_d = (_c = parts.find((p) => p.type === "day")) == null ? void 0 : _c.value) != null ? _d : "";
    if (/[一-鿿]/.test(monthStr)) {
      const dayNum = parseInt(dayStr);
      if (!isNaN(dayNum) && dayNum >= 1 && dayNum <= 30) {
        const LUNAR_DAYS = [
          "\u521D\u4E00",
          "\u521D\u4E8C",
          "\u521D\u4E09",
          "\u521D\u56DB",
          "\u521D\u4E94",
          "\u521D\u516D",
          "\u521D\u4E03",
          "\u521D\u516B",
          "\u521D\u4E5D",
          "\u521D\u5341",
          "\u5341\u4E00",
          "\u5341\u4E8C",
          "\u5341\u4E09",
          "\u5341\u56DB",
          "\u5341\u4E94",
          "\u5341\u516D",
          "\u5341\u4E03",
          "\u5341\u516B",
          "\u5341\u4E5D",
          "\u4E8C\u5341",
          "\u5EFF\u4E00",
          "\u5EFF\u4E8C",
          "\u5EFF\u4E09",
          "\u5EFF\u56DB",
          "\u5EFF\u4E94",
          "\u5EFF\u516D",
          "\u5EFF\u4E03",
          "\u5EFF\u516B",
          "\u5EFF\u4E5D",
          "\u4E09\u5341"
        ];
        return monthStr + ((_e = LUNAR_DAYS[dayNum - 1]) != null ? _e : dayStr);
      }
      return monthStr + dayStr.replace("\u65E5", "");
    }
    const m = parseInt(monthStr) || 1;
    const day = parseInt(dayStr) || 1;
    const MONTHS = ["\u6B63\u6708", "\u4E8C\u6708", "\u4E09\u6708", "\u56DB\u6708", "\u4E94\u6708", "\u516D\u6708", "\u4E03\u6708", "\u516B\u6708", "\u4E5D\u6708", "\u5341\u6708", "\u51AC\u6708", "\u814A\u6708"];
    const DAYS = ["\u521D\u4E00", "\u521D\u4E8C", "\u521D\u4E09", "\u521D\u56DB", "\u521D\u4E94", "\u521D\u516D", "\u521D\u4E03", "\u521D\u516B", "\u521D\u4E5D", "\u521D\u5341", "\u5341\u4E00", "\u5341\u4E8C", "\u5341\u4E09", "\u5341\u56DB", "\u5341\u4E94", "\u5341\u516D", "\u5341\u4E03", "\u5341\u516B", "\u5341\u4E5D", "\u4E8C\u5341", "\u5EFF\u4E00", "\u5EFF\u4E8C", "\u5EFF\u4E09", "\u5EFF\u56DB", "\u5EFF\u4E94", "\u5EFF\u516D", "\u5EFF\u4E03", "\u5EFF\u516B", "\u5EFF\u4E5D", "\u4E09\u5341"];
    return MONTHS[m - 1] + ((_f = DAYS[day - 1]) != null ? _f : "");
  } catch (e) {
    return "";
  }
}
var _DashboardView = class _DashboardView extends import_obsidian5.ItemView {
  constructor(leaf, plugin) {
    super(leaf);
    this.boardEl = null;
    this.noiseId = null;
    this.pulseEls = null;
    this.dateEl = null;
    this.adTitleEl = null;
    this.weekdayEl = null;
    this.lunarEl = null;
    this.dashboardEl = null;
    this.adThemeBtn = null;
    this.adBoardWired = false;
    this.adLastColCount = 0;
    this.calYear = 0;
    this.calMonth = 0;
    this.weatherCache = null;
    this.wxTimer = null;
    this.kanbanRefreshTimer = null;
    this.sitesRefreshTimer = null;
    this.docsRefreshTimer = null;
    /** 番茄钟：倒计时 tick 定时器（setInterval 句柄） */
    this.pomoTimer = null;
    /** 番茄钟运行时状态（内存态，不落盘；时长/计数等配置见 settings.pomodoro） */
    this.pomo = {
      /** 当前阶段：work 专注 / short 短休 / long 长休 */
      mode: "work",
      /** 是否正在倒计时 */
      running: false,
      /** 本次阶段结束的绝对时间戳（ms） */
      endAt: 0,
      /** 本次阶段总时长（ms），用于绘制进度环 */
      duration: 0,
      /** 当前阶段剩余时间（ms）；暂停时保持，运行时由 endAt 推算 */
      remaining: 0
    };
    /** 网站收藏：被折叠的分组名（会话内保持，点击分类标题展开/折叠） */
    this.sitesCollapsed = /* @__PURE__ */ new Set();
    this.homeModules = [
      { id: "todo", title: "\u5F85\u529E \xB7 \u4ECA\u5929\u8981\u5904\u7406", cardCls: "ad-card ad-b-todo", live: false, render: (b) => void this.renderKanbanTodo(b) },
      { id: "weather", title: "\u5929\u6C14", cardCls: "ad-card ad-b-weather", live: false, render: (b) => void this.renderWeather(b) },
      { id: "calendar", title: "\u65E5\u5386", cardCls: "ad-card ad-b-calendar", live: false, render: (b) => this.renderCalendar(b) },
      { id: "sites", title: "\u7F51\u7AD9\u6536\u85CF", cardCls: "ad-card ad-b-sites", live: false, render: (b) => void this.renderSites(b) },
      { id: "favorites", title: "\u5E38\u7528\u6587\u6863", cardCls: "ad-card ad-b-fav", live: false, render: (b) => this.renderFavorites(b) },
      { id: "quick-capture", title: "\u7075\u611F\u6355\u6349", cardCls: "ad-card ad-b-capture", live: false, render: (b) => this.renderQuickCapture(b) },
      { id: "pomodoro", title: "\u756A\u8304\u949F", cardCls: "ad-card ad-b-pomodoro", live: false, render: (b) => this.renderPomodoro(b) }
    ];
    this.currentPage = "home";
    this.plugin = plugin;
  }
  async onOpen() {
    var _a, _b;
    this.containerEl.empty();
    this.dashboardEl = this.containerEl.createDiv({ cls: "dashboard-plugin" });
    this.applyTheme();
    this.registerEvent(this.app.workspace.on("css-change", () => this.applyTheme()));
    try {
      const d = MOCK_DATA;
      this.renderNoise(this.dashboardEl);
      void this.renderPulse(this.dashboardEl, d);
      this.renderHeader(this.dashboardEl, d);
      this.renderActions(this.dashboardEl);
      this.renderBoard(this.dashboardEl, d);
      const refreshAll = () => {
        void this.updatePulse();
        this.scheduleKanbanRefresh();
      };
      this.registerEvent(this.app.vault.on("create", refreshAll));
      this.registerEvent(this.app.vault.on("delete", refreshAll));
      this.registerEvent(this.app.vault.on("rename", refreshAll));
      this.registerEvent(this.app.vault.on("modify", (file) => {
        if (!(file instanceof import_obsidian5.TFile)) return;
        if (file.path === this.plugin.settings.kanbanFile) {
          this.scheduleKanbanRefresh();
          return;
        }
        if (file.path === this.plugin.settings.favoriteSitesFile) {
          this.scheduleSitesRefresh();
          return;
        }
        if (file.path === this.plugin.settings.favoriteDocsFile) {
          this.scheduleDocsRefresh();
          return;
        }
        void this.updatePulse();
      }));
    } catch (err) {
      try {
        const e = err instanceof Error ? err : new Error(String(err));
        (_a = this.dashboardEl) == null ? void 0 : _a.empty();
        (_b = this.dashboardEl) == null ? void 0 : _b.createEl("pre", { cls: "ad-error", text: "Dashboard render error: " + (e.message || "") });
      } catch (e) {
      }
      console.error("[Dashboard] render error", err);
    }
  }
  async onClose() {
    var _a;
    if (this.noiseId) {
      window.cancelAnimationFrame(this.noiseId);
      this.noiseId = null;
    }
    if (this.adRowHObs) {
      this.adRowHObs.disconnect();
      this.adRowHObs = void 0;
    }
    if (this.wxTimer !== null) {
      window.clearInterval(this.wxTimer);
      this.wxTimer = null;
    }
    if (this.kanbanRefreshTimer !== null) {
      window.clearTimeout(this.kanbanRefreshTimer);
      this.kanbanRefreshTimer = null;
    }
    if (this.sitesRefreshTimer !== null) {
      window.clearTimeout(this.sitesRefreshTimer);
      this.sitesRefreshTimer = null;
    }
    if (this.docsRefreshTimer !== null) {
      window.clearTimeout(this.docsRefreshTimer);
      this.docsRefreshTimer = null;
    }
    if (this.pomoTimer !== null) {
      window.clearInterval(this.pomoTimer);
      this.pomoTimer = null;
    }
    (_a = this.dashboardEl) == null ? void 0 : _a.empty();
  }
  /** 按当前语言返回中 / 英文案（主界面文案双语） */
  t(zh, en) {
    return this.plugin.settings.language === "en" ? en : zh;
  }
  effectiveTheme() {
    const t = this.plugin.settings.theme;
    if (t === "auto") return document.body.classList.contains("theme-light") ? "light" : "dark";
    return t;
  }
  applyTheme() {
    var _a2;
    const root = (_a2 = this.dashboardEl) != null ? _a2 : this.containerEl.querySelector(".dashboard-plugin");
    if (root) root.setAttribute("data-theme", this.effectiveTheme());
    this.refreshThemeButton();
  }
  /** Keep the header toggle's icon/tooltip in sync with the effective theme. */
  refreshThemeButton() {
    const btn = this.adThemeBtn;
    if (!btn) return;
    const eff = this.effectiveTheme();
    btn.textContent = eff === "dark" ? "\u2600" : "\u{1F319}";
    btn.title = (eff === "dark" ? "\u5207\u6362\u5230\u6D45\u8272" : "\u5207\u6362\u5230\u6DF1\u8272") + "\uFF08\u540C\u65F6\u5207\u6362 Obsidian \u5916\u89C2\uFF09";
  }
  getViewType() {
    return VIEW_TYPE;
  }
  getDisplayText() {
    return "Dashboard";
  }
  getIcon() {
    return "layout-dashboard";
  }
  renderNoise(root) {
    const canvas = root.createEl("canvas", { cls: "ad-noise" });
    canvas.setCssProps({
      position: "absolute",
      inset: "0",
      width: "100%",
      height: "100%",
      zIndex: "0",
      pointerEvents: "none",
      imageRendering: "pixelated",
      display: "block"
    });
    const ctx = canvas.getContext("2d", { alpha: true });
    if (!ctx) return;
    const size = 1024;
    canvas.width = size;
    canvas.height = size;
    ctx.imageSmoothingEnabled = false;
    let frame = 0;
    const draw = () => {
      if (frame % 2 === 0) {
        const img = ctx.createImageData(size, size);
        const d = img.data;
        for (let i = 0; i < d.length; i += 4) {
          const v = Math.random() * 255;
          d[i] = v;
          d[i + 1] = v;
          d[i + 2] = v;
          d[i + 3] = 18;
        }
        ctx.putImageData(img, 0, 0);
      }
      frame++;
      this.noiseId = window.requestAnimationFrame(draw);
    };
    this.noiseId = window.requestAnimationFrame(draw);
  }
  /* ============================================================
     Pulse
     ============================================================ */
  getVaultNoteCounts() {
    var _a2;
    const counts = /* @__PURE__ */ new Map();
    const files = this.app.vault.getMarkdownFiles();
    for (const file of files) {
      const d = new Date(file.stat.ctime);
      const key = fmtDate(d);
      counts.set(key, ((_a2 = counts.get(key)) != null ? _a2 : 0) + 1);
    }
    return counts;
  }
  async renderPulse(root, d) {
    var _a2;
    const bar = root.createDiv({ cls: "ad-pulse" });
    bar.createSpan({ cls: "ad-pulse__tag", text: "[ VAULT PULSE ]" });
    const today = /* @__PURE__ */ new Date();
    const todayKey = todayStr();
    const noteCounts = this.getVaultNoteCounts();
    const hs = calcHeatmapStats(noteCounts, today.getFullYear(), today);
    const todayCount = (_a2 = noteCounts.get(todayKey)) != null ? _a2 : 0;
    const totalEl = bar.createSpan({ text: `${hs.total} NOTES` });
    bar.createSpan({ cls: "ad-pulse__sep", text: "\xB7" });
    const todayEl = bar.createSpan();
    todayEl.textContent = `\u0394 TODAY +${todayCount}`;
    bar.createSpan({ cls: "ad-pulse__sep", text: "\xB7" });
    const streakEl = bar.createSpan({ text: `${hs.streak}D STREAK` });
    const caret = bar.createSpan({ cls: "ad-pulse__caret" });
    let caretOn = true;
    this.registerInterval(window.setInterval(() => {
      caretOn = !caretOn;
      caret.style.opacity = caretOn ? "1" : "0";
    }, 525));
    this.pulseEls = { total: totalEl, today: todayEl, streak: streakEl };
  }
  async updatePulse() {
    var _a2;
    if (!this.pulseEls) return;
    const today = /* @__PURE__ */ new Date();
    const todayKey = todayStr();
    const noteCounts = this.getVaultNoteCounts();
    const hs = calcHeatmapStats(noteCounts, today.getFullYear(), today);
    const todayCount = (_a2 = noteCounts.get(todayKey)) != null ? _a2 : 0;
    this.pulseEls.total.textContent = `${hs.total} NOTES`;
    this.pulseEls.today.textContent = `\u0394 TODAY +${todayCount}`;
    this.pulseEls.streak.textContent = `${hs.streak}D STREAK`;
  }
  /** Live-update only the dashboard title text (cheap; no full re-render). */
  renderHeader(root, d) {
    var _a2, _b;
    const h = root.createEl("header", { cls: "ad-header" });
    const left = h.createDiv({ cls: "ad-header__left" });
    left.createEl("p", { cls: "ad-eyebrow", text: d.header.eyebrow });
    this.adTitleEl = left.createEl("h1", { cls: "ad-title", text: this.plugin.settings.dashboardTitle || d.header.title });
    left.createEl("p", { cls: "ad-subtitle", text: "Obsidian \xB7 Personal Dashboard \xB7 v" + ((_b = (_a2 = this.plugin.manifest) == null ? void 0 : _a2.version) != null ? _b : d.header.subtitle.replace(/^.*v/, "v")) });
    const right = h.createDiv({ cls: "ad-header__right" });
    const now = /* @__PURE__ */ new Date();
    const isEn = this.plugin.settings.language === "en";
    const dateStr = now.toLocaleDateString(isEn ? "en-US" : "zh-CN", { timeZone: "Asia/Shanghai", year: "numeric", month: isEn ? "short" : "2-digit", day: "numeric" });
    const timeStr = now.toLocaleTimeString(isEn ? "en-US" : "zh-CN", { timeZone: "Asia/Shanghai", hour: "2-digit", minute: "2-digit" });
    this.dateEl = right.createDiv({ cls: "ad-header__date", text: `${dateStr} ${timeStr}` });
    const meta = right.createDiv({ cls: "ad-header__meta" });
    this.weekdayEl = meta.createSpan({ text: (/* @__PURE__ */ new Date()).toLocaleDateString(isEn ? "en-US" : "zh-CN", { timeZone: "Asia/Shanghai", weekday: "long" }) });
    meta.createSpan({ cls: "ad-dot" });
    const initialLunar = getLunarDate(/* @__PURE__ */ new Date());
    this.lunarEl = this.plugin.settings.language === "en" ? null : meta.createSpan({ text: initialLunar ? "\u519C\u5386 " + initialLunar : d.lunar });
    const btns = right.createDiv({ cls: "ad-header__btns" });
    const themeBtn = btns.createEl("button", { cls: "ad-header__theme" });
    this.adThemeBtn = themeBtn;
    this.refreshThemeButton();
    themeBtn.addEventListener("click", () => {
      void (async () => {
        const next = this.effectiveTheme() === "light" ? "dark" : "light";
        this.plugin.setObsidianTheme(next);
        this.plugin.settings.theme = "auto";
        await this.plugin.saveSettings();
        this.plugin.refreshThemeButtons();
        this.applyTheme();
      })();
    });
    const settings = btns.createEl("button", { cls: "ad-header__settings" });
    settings.textContent = this.t("\u2699 \u8BBE\u7F6E", "\u2699 Settings");
    settings.addEventListener("click", () => {
      var _a3, _b2;
      const app = this.app;
      (_a3 = app.setting) == null ? void 0 : _a3.open();
      (_b2 = app.setting) == null ? void 0 : _b2.openTabById(this.plugin.manifest.id);
    });
    this.registerInterval(window.setInterval(() => {
      const n = /* @__PURE__ */ new Date();
      if (this.dateEl) {
        const ds = n.toLocaleDateString("zh-CN", { timeZone: "Asia/Shanghai", year: "numeric", month: "2-digit", day: "2-digit" });
        const ts = n.toLocaleTimeString("zh-CN", { timeZone: "Asia/Shanghai", hour: "2-digit", minute: "2-digit" });
        this.dateEl.textContent = `${ds} ${ts}`;
      }
      if (this.weekdayEl) {
        this.weekdayEl.textContent = n.toLocaleDateString(this.plugin.settings.language === "en" ? "en-US" : "zh-CN", { timeZone: "Asia/Shanghai", weekday: "long" });
      }
      if (this.lunarEl) {
        const lunar = getLunarDate(n);
        if (lunar) this.lunarEl.textContent = "\u519C\u5386 " + lunar;
      }
    }, 3e4));
  }
  /* ============================================================
     Actions toolbar
     ============================================================ */
  refreshTitle() {
    if (!this.adTitleEl) return;
    this.adTitleEl.textContent = this.plugin.settings.dashboardTitle || MOCK_DATA.header.title;
  }
  /* ============================================================
     Header
     ============================================================ */
  renderActions(root) {
    const nav = root.createEl("nav", { cls: "ad-toolbar" });
    const navItems = [
      { glyph: "\u2302", label: this.t("\u4E3B\u9875", "Home"), action: "home", svg: ICON_home }
    ];
    const makeBtn = (it, extraCls = "") => {
      const btn = nav.createEl("button", { cls: "ad-toolbar__btn" + (extraCls ? " " + extraCls : "") });
      const glyphEl = btn.createSpan({ cls: "ad-glyph" });
      if (it.svg) injectSvg(glyphEl, it.svg);
      else glyphEl.textContent = it.glyph;
      btn.createSpan({ text: it.label });
      btn.addEventListener("click", () => {
        btn.addClass("is-active");
        try {
          if (it.action === "home") void this.showDashboard();
        } catch (e) {
          const msg = e instanceof Error ? e.message : String(e);
          this.showToast(this.t("\u6253\u5F00\u5931\u8D25\uFF1A", "Failed to open: ") + msg, "error");
          console.error('[Dashboard] toolbar action "' + it.action + '" failed', e);
        }
        window.setTimeout(() => btn.removeClass("is-active"), 350);
      });
      return btn;
    };
    const navGroup = nav.createDiv({ cls: "ad-toolbar__group" });
    navItems.forEach((it) => navGroup.appendChild(makeBtn(it)));
  }
  /* ============================================================
     Parse-issue banner (shown directly under the banner image)
     ============================================================ */
  async openFileByPath(path) {
    const f = this.app.vault.getAbstractFileByPath(path);
    if (f instanceof import_obsidian5.TFile) {
      const leaf = this.app.workspace.getLeaf(true);
      await leaf.openFile(f);
    } else {
      this.showToast(this.t("\u6587\u4EF6\u4E0D\u5B58\u5728\uFF1A", "File not found: ") + path, "error");
    }
  }
  /* ============================================================
     Empty-state helper + first-run guide (no sample-data auto-create)
     ============================================================ */
  renderEmpty(container, opts) {
    const e = container.createDiv({ cls: "ad-empty" });
    if (opts.icon) e.createDiv({ cls: "ad-empty__icon", text: opts.icon });
    e.createDiv({ cls: "ad-empty__title", text: opts.title });
    if (opts.hint) e.createDiv({ cls: "ad-empty__hint", text: opts.hint });
    if (opts.actionLabel && opts.onAction) {
      const btn = e.createEl("button", { cls: "ad-empty__btn", text: opts.actionLabel });
      btn.addEventListener("click", () => opts.onAction());
    }
  }
  renderBoard(root, d) {
    const board = root.createDiv({ cls: "ad-board" });
    this.boardEl = board;
    void this.renderEnabledModules(board);
    this.attachBoardInteractions();
  }
  /* ---- Quick Capture ---- */
  rebuildHome() {
    if (this.currentPage !== "home" || !this.boardEl) return;
    this.boardEl.empty();
    void this.renderEnabledModules(this.boardEl);
  }
  /** 设置页修改看板开关/名称/阶段配置后，立即刷新导航与看板页（无需重启） */
  renderQuickCapture(board) {
    const card = this.getOrCreateCard(board, "ad-card ad-b-capture");
    this.cardHead(card, "\u25C6", this.t("\u5FEB\u901F\u6355\u6349", "Quick Capture"));
    const qc = card.createDiv({ cls: "ad-qc" });
    const area = qc.createEl("textarea", {
      cls: "ad-qc__area",
      attr: { rows: "3", placeholder: this.t("\u8BB0\u5F55\u4E00\u95EA\u800C\u8FC7\u7684\u60F3\u6CD5\u2026", "Capture a fleeting thought\u2026") }
    });
    const row = qc.createDiv({ cls: "ad-qc__row" });
    const cta = row.createEl("button", { cls: "ad-qc__cta", text: this.t("\u6355\u6349", "Capture") });
    const submit = async () => {
      const content = area.value.trim();
      if (!content) {
        area.focus();
        return;
      }
      cta.addClass("flash");
      try {
        await this.createCaptureNote(content);
        area.value = "";
        this.showToast(this.t("\u2728 \u60F3\u6CD5\u5DF2\u6355\u6349\uFF01", "Captured!"));
      } catch (err) {
        console.error("[Dashboard] \u5FEB\u901F\u6355\u6349\u5931\u8D25", err);
        this.showToast(this.t("\u26A0\uFE0F \u6355\u6349\u5931\u8D25\uFF0C\u8BF7\u68C0\u67E5\u300C\u5B58\u50A8\u8DEF\u5F84\u300D\u8BBE\u7F6E", "Capture failed, check Storage Paths"), "error");
      } finally {
        window.setTimeout(() => cta.removeClass("flash"), 400);
      }
    };
    cta.addEventListener("click", () => void submit());
  }
  /** 读取今日番茄计数，跨天自动归零并落盘 */
  pomoDailyCount() {
    const p = this.plugin.settings.pomodoro;
    const today = todayStr();
    if (p.dailyDate !== today) {
      p.dailyCount = 0;
      p.dailyDate = today;
      void this.plugin.saveSettings();
    }
    return p.dailyCount || 0;
  }
  /** 当前阶段的总时长（ms），取自设置 */
  pomoDurationMs() {
    const p = this.plugin.settings.pomodoro;
    const minutes = this.pomo.mode === "work" ? p.workMinutes : this.pomo.mode === "short" ? p.shortBreakMinutes : p.longBreakMinutes;
    return (minutes || 25) * 60 * 1e3;
  }
  /** 剩余毫秒 → mm:ss */
  pomoFmt(ms) {
    const total = Math.max(0, Math.ceil(ms / 1e3));
    const m = Math.floor(total / 60);
    const s = total % 60;
    return String(m).padStart(2, "0") + ":" + String(s).padStart(2, "0");
  }
  /** 当前阶段的中英文标签 */
  pomoModeLabel() {
    if (this.pomo.mode === "short") return this.t("\u77ED\u4F11\u606F", "Short Break");
    if (this.pomo.mode === "long") return this.t("\u957F\u4F11\u606F", "Long Break");
    return this.t("\u4E13\u6CE8\u4E2D", "Focus");
  }
  /** 启动每秒 tick（幂等：已在运行则忽略） */
  pomoEnsureTimer() {
    if (this.pomoTimer !== null) return;
    this.pomoTimer = window.setInterval(() => this.pomoTick(), 1e3);
  }
  /** 停止 tick */
  pomoStopTimer() {
    if (this.pomoTimer !== null) {
      window.clearInterval(this.pomoTimer);
      this.pomoTimer = null;
    }
  }
  /** 每秒跳动：更新时间与进度环；到点则完成当前阶段 */
  pomoTick() {
    if (!this.pomo.running) return;
    const remain = this.pomo.endAt - Date.now();
    if (remain <= 0) {
      this.pomoComplete();
      return;
    }
    this.pomo.remaining = remain;
    this.pomoUpdateUI();
  }
  /** 把当前计时状态同步到卡片 DOM（时间 / 模式 / 进度环 / 按钮文本） */
  pomoUpdateUI() {
    const root = this.boardEl;
    if (!root) return;
    const C = 2 * Math.PI * _DashboardView.POMO_R;
    const body = root.querySelector(".ad-pomo");
    if (body) {
      body.classList.toggle("ad-pomo--work", this.pomo.mode === "work");
      body.classList.toggle("ad-pomo--short", this.pomo.mode === "short");
      body.classList.toggle("ad-pomo--long", this.pomo.mode === "long");
    }
    const time = root.querySelector(".ad-pomo__time");
    if (time) time.textContent = this.pomoFmt(this.pomo.remaining);
    const mode = root.querySelector(".ad-pomo__mode");
    if (mode) mode.textContent = this.pomoModeLabel();
    const stage = root.querySelector(".ad-pomo__stage");
    if (stage) {
      stage.textContent = this.pomo.mode === "work" ? this.t("\u7B2C " + (this.pomoDailyCount() + 1) + " \u4E2A\u756A\u8304", "Pomodoro #" + (this.pomoDailyCount() + 1)) : this.t("\u672C\u8F6E\u5DF2\u5B8C\u6210 " + this.pomoDailyCount() + " \u4E2A", this.pomoDailyCount() + " done this round");
    }
    const fg = root.querySelector(".ad-pomo__ring-fg");
    if (fg instanceof SVGCircleElement) {
      const ratio = this.pomo.duration > 0 ? this.pomo.remaining / this.pomo.duration : 1;
      fg.style.strokeDasharray = String(C);
      fg.style.strokeDashoffset = String(C * ratio);
    }
    const btn = root.querySelector(".ad-pomo__btn-start");
    if (btn) btn.textContent = this.pomo.running ? this.t("\u6682\u505C", "Pause") : this.t("\u5F00\u59CB", "Start");
  }
  /** 开始 / 继续倒计时 */
  pomoStart() {
    if (this.pomo.remaining <= 0) this.pomoReset();
    this.pomo.running = true;
    this.pomo.endAt = Date.now() + this.pomo.remaining;
    this.pomoEnsureTimer();
    this.pomoUpdateUI();
  }
  /** 暂停倒计时 */
  pomoPause() {
    this.pomo.running = false;
    this.pomo.remaining = Math.max(0, this.pomo.endAt - Date.now());
    this.pomoStopTimer();
    this.pomoUpdateUI();
  }
  /** 重置：回到当前阶段完整时长并停止 */
  pomoReset() {
    this.pomo.running = false;
    this.pomoStopTimer();
    this.pomo.duration = this.pomoDurationMs();
    this.pomo.remaining = this.pomo.duration;
    this.pomo.endAt = Date.now() + this.pomo.duration;
    this.pomoUpdateUI();
  }
  /** 当前阶段结束（自然到点或点击跳过）：专注 → 计数并进入休息；休息 → 回到专注 */
  pomoComplete() {
    const p = this.plugin.settings.pomodoro;
    if (this.pomo.mode === "work") {
      if (p.dailyDate !== todayStr()) {
        p.dailyCount = 0;
        p.dailyDate = todayStr();
      }
      p.dailyCount = (p.dailyCount || 0) + 1;
      p.dailyDate = todayStr();
      void this.plugin.saveSettings();
      this.pomo.mode = p.dailyCount % (p.cycles || 4) === 0 ? "long" : "short";
      this.showToast(this.t("\u{1F345} \u4E13\u6CE8\u5B8C\u6210\uFF01\u4F11\u606F\u4E00\u4E0B", "\u{1F345} Focus done! Take a break"));
    } else {
      this.pomo.mode = "work";
      this.showToast(this.t("\u2615 \u4F11\u606F\u7ED3\u675F\uFF0C\u5F00\u59CB\u4E13\u6CE8", "\u2615 Break over, back to focus"));
    }
    this.pomo.duration = this.pomoDurationMs();
    this.pomo.remaining = this.pomo.duration;
    this.pomo.running = p.autoStart;
    this.pomo.endAt = Date.now() + this.pomo.remaining;
    if (p.sound) this.pomoBeep();
    if (this.pomo.running) this.pomoEnsureTimer();
    else this.pomoStopTimer();
    if (this.boardEl) this.renderPomodoro(this.boardEl);
  }
  /** 结束提示音：Web Audio 两声短音（无需资源文件） */
  pomoBeep() {
    try {
      const Ctor = window.AudioContext || window.webkitAudioContext;
      if (!Ctor) return;
      const ctx = new Ctor();
      const now = ctx.currentTime;
      const beep = (t) => {
        const osc = ctx.createOscillator();
        const gain = ctx.createGain();
        osc.type = "sine";
        osc.frequency.value = 880;
        gain.gain.setValueAtTime(1e-4, t);
        gain.gain.exponentialRampToValueAtTime(0.2, t + 0.02);
        gain.gain.exponentialRampToValueAtTime(1e-4, t + 0.35);
        osc.connect(gain);
        gain.connect(ctx.destination);
        osc.start(t);
        osc.stop(t + 0.4);
      };
      beep(now);
      beep(now + 0.45);
    } catch (err) {
    }
  }
  /** 番茄钟卡片渲染：环形进度 + 剩余时间 + 控制按钮 + 今日计数 */
  renderPomodoro(board) {
    const card = this.getOrCreateCard(board, "ad-card ad-b-pomodoro");
    const daily = this.pomoDailyCount();
    const hint = card.createSpan({ cls: "ad-card__hint ad-pomo__count" });
    this.cardHead(card, "\u25C9", this.t("\u756A\u8304\u949F", "Pomodoro"), void 0, hint);
    hint.textContent = this.t("\u4ECA\u65E5 ", "Today ") + daily + " \u{1F345}";
    if (!this.pomo.duration) {
      this.pomo.duration = this.pomoDurationMs();
      this.pomo.remaining = this.pomo.duration;
      this.pomo.endAt = Date.now() + this.pomo.duration;
    }
    const body = card.createDiv({ cls: "ad-pomo" });
    const ringWrap = body.createDiv({ cls: "ad-pomo__ringwrap" });
    const NS = "http://www.w3.org/2000/svg";
    const svg = document.createElementNS(NS, "svg");
    svg.setAttribute("viewBox", "0 0 120 120");
    svg.setAttribute("class", "ad-pomo__ring");
    const bg = document.createElementNS(NS, "circle");
    bg.setAttribute("class", "ad-pomo__ring-bg");
    bg.setAttribute("cx", "60");
    bg.setAttribute("cy", "60");
    bg.setAttribute("r", String(_DashboardView.POMO_R));
    const fg = document.createElementNS(NS, "circle");
    fg.setAttribute("class", "ad-pomo__ring-fg");
    fg.setAttribute("cx", "60");
    fg.setAttribute("cy", "60");
    fg.setAttribute("r", String(_DashboardView.POMO_R));
    svg.appendChild(bg);
    svg.appendChild(fg);
    ringWrap.appendChild(svg);
    const center = ringWrap.createDiv({ cls: "ad-pomo__center" });
    center.createDiv({ cls: "ad-pomo__time", text: this.pomoFmt(this.pomo.remaining) });
    center.createDiv({ cls: "ad-pomo__mode" });
    const ctrl = body.createDiv({ cls: "ad-pomo__ctrl" });
    const start = ctrl.createEl("button", { cls: "ad-pomo__btn ad-pomo__btn-start" });
    const reset = ctrl.createEl("button", { cls: "ad-pomo__btn", text: this.t("\u91CD\u7F6E", "Reset") });
    const skip = ctrl.createEl("button", { cls: "ad-pomo__btn", text: this.t("\u8DF3\u8FC7", "Skip") });
    start.addEventListener("click", () => {
      if (this.pomo.running) this.pomoPause();
      else this.pomoStart();
    });
    reset.addEventListener("click", () => this.pomoReset());
    skip.addEventListener("click", () => this.pomoComplete());
    const foot = body.createDiv({ cls: "ad-pomo__foot" });
    const dots = foot.createDiv({ cls: "ad-pomo__dots" });
    for (let i = 0; i < (this.plugin.settings.pomodoro.cycles || 4); i++) {
      dots.createSpan({ cls: "ad-pomo__dot" + (i < daily ? " is-done" : "") });
    }
    foot.createDiv({ cls: "ad-pomo__stage" });
    this.pomoUpdateUI();
    if (this.pomo.running) this.pomoEnsureTimer();
  }
  /* ---- Toast ---- */
  showToast(message, kind = "success") {
    const toast = document.body.createDiv({ cls: "ad-toast" + (kind === "error" ? " ad-toast--error" : "") });
    toast.createSpan({ text: message });
    window.setTimeout(() => {
      toast.addClass("ad-toast--out");
      window.setTimeout(() => toast.remove(), 300);
    }, 2500);
  }
  /* ---- Create note in vault ---- */
  /** Ensure a folder exists, creating parent folders recursively if needed. */
  async ensureFolder(path) {
    if (!path || path === "/") return;
    if (this.app.vault.getAbstractFileByPath(path)) return;
    const parts = path.split("/").filter(Boolean);
    let cur = "";
    for (const part of parts) {
      cur = cur ? `${cur}/${part}` : part;
      if (!this.app.vault.getAbstractFileByPath(cur)) {
        await this.app.vault.createFolder(cur);
      }
    }
  }
  async createCaptureNote(content) {
    const qc = this.plugin.settings.quickCapture;
    const now = /* @__PURE__ */ new Date();
    const folderPath = qc.storagePath;
    await this.ensureFolder(folderPath);
    const filename = this.applyNamingPattern(qc.namingPattern, now);
    const filepath = `${folderPath}/${filename}.md`;
    let fileContent = content;
    if (qc.templateFile) {
      const tplPath = this.resolveTemplatePath(qc.templateFile);
      const tplFile = this.app.vault.getAbstractFileByPath(tplPath);
      if (tplFile instanceof import_obsidian5.TFile) {
        const tpl = await this.app.vault.read(tplFile);
        fileContent = this.applyTemplate(tpl, content, filename, now);
      }
    }
    await this.app.vault.create(filepath, fileContent);
  }
  /* ---- Create diary note ---- */
  applyTemplate(template, content, title, d) {
    const pad = (n) => String(n).padStart(2, "0");
    const date = `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())}`;
    const time = `${pad(d.getHours())}:${pad(d.getMinutes())}`;
    let result = template.replace(/\{\{date\}\}/g, date).replace(/\{\{time\}\}/g, time).replace(/\{\{title\}\}/g, title);
    if (result.includes("{{content}}")) {
      result = result.replace(/\{\{content\}\}/g, content);
    } else {
      result += "\n\n" + content;
    }
    return result;
  }
  resolveTemplatePath(file) {
    const f = file.trim();
    if (!f) return "";
    return f.endsWith(".md") ? f : `${f}.md`;
  }
  applyNamingPattern(pattern, d) {
    const pad = (n) => String(n).padStart(2, "0");
    const isEn = this.plugin.settings.language === "en";
    const WK_SHORT = isEn ? ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"] : ["\u5468\u65E5", "\u5468\u4E00", "\u5468\u4E8C", "\u5468\u4E09", "\u5468\u56DB", "\u5468\u4E94", "\u5468\u516D"];
    const WK_FULL = isEn ? ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"] : ["\u661F\u671F\u65E5", "\u661F\u671F\u4E00", "\u661F\u671F\u4E8C", "\u661F\u671F\u4E09", "\u661F\u671F\u56DB", "\u661F\u671F\u4E94", "\u661F\u671F\u516D"];
    const meridiem = d.getHours() < 12 ? isEn ? "AM" : "\u4E0A\u5348" : isEn ? "PM" : "\u4E0B\u5348";
    const h12 = d.getHours() % 12 || 12;
    const map = {
      YYYY: String(d.getFullYear()),
      MMM: isEn ? ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"][d.getMonth()] : `${d.getMonth() + 1}\u6708`,
      MM: pad(d.getMonth() + 1),
      dddd: WK_FULL[d.getDay()],
      ddd: WK_SHORT[d.getDay()],
      DD: pad(d.getDate()),
      HH: pad(d.getHours()),
      hh: pad(h12),
      mm: pad(d.getMinutes()),
      ss: pad(d.getSeconds()),
      SS: pad(d.getSeconds()),
      A: meridiem
    };
    const name = pattern.replace(/(dddd|ddd|YYYY|MMM|MM|DD|HH|hh|mm|ss|SS|A)/g, (m) => {
      var _a2;
      return (_a2 = map[m]) != null ? _a2 : m;
    });
    return name.replace(/[*"/<>:|?\\]/g, "-");
  }
  /* ============================================================
     Task actions
     ============================================================ */
  /** Toggle task status in source file's Chinese frontmatter */
  async showDashboard() {
    if (!this.boardEl) return;
    this.boardEl.empty();
    this.boardEl.removeClass("po-board");
    this.boardEl.removeClass("op-board");
    this.boardEl.addClass("ad-board");
    this.currentPage = "home";
    await this.renderEnabledModules(this.boardEl);
  }
  /** Delete task file from vault */
  static cardSel(cls) {
    return "." + cls.trim().split(/\s+/).join(".");
  }
  /** Reuse an existing card element (keeps its grid placement → no disappearance flash)
   *  by emptying its contents, or create it if missing. */
  getOrCreateCard(board, cls) {
    const existing = board.querySelector(_DashboardView.cardSel(cls));
    let card;
    if (existing) {
      existing.empty();
      card = existing;
    } else {
      card = board.createDiv({ cls });
    }
    return card;
  }
  async renderEnabledModules(board, opts) {
    var _a2;
    const configs = (_a2 = this.plugin.settings.homeModules) != null ? _a2 : [];
    const enabled = configs.filter((m) => m.enabled && this.homeModules.some((x) => x.id === m.id)).sort((a, b) => a.order - b.order);
    const enabledTokens = enabled.map((m) => {
      const mod = this.homeModules.find((x) => x.id === m.id);
      return mod ? mod.cardCls.split(" ")[1] : "";
    }).filter((t) => t !== "");
    board.querySelectorAll(".ad-card").forEach((el) => {
      const matched = enabledTokens.some((tok) => el.classList.contains(tok));
      if (!matched) el.remove();
    });
    const shells = [];
    for (const cfg of enabled) {
      const mod = this.homeModules.find((x) => x.id === cfg.id);
      if (!mod) continue;
      const sel = _DashboardView.cardSel(mod.cardCls);
      let el = board.querySelector(sel);
      if (!el) el = board.createDiv({ cls: mod.cardCls });
      el.setAttribute("data-mod", mod.id);
      this.applyCardSpan(el, cfg.cols, cfg.rows);
      shells.push(el);
    }
    let prev = null;
    for (const el of shells) {
      const expected = prev ? prev.nextElementSibling : board.firstElementChild;
      if (expected !== el) board.insertBefore(el, expected);
      prev = el;
    }
    for (const cfg of enabled) {
      const mod = this.homeModules.find((x) => x.id === cfg.id);
      if (!mod) continue;
      if ((opts == null ? void 0 : opts.onlyLive) && mod.live === false) continue;
      if (this.currentPage !== "home" || !this.boardEl) return;
      await mod.render(board);
      const cardEl = board.querySelector(_DashboardView.cardSel(mod.cardCls));
      if (cardEl) {
        cardEl.setAttribute("data-mod", mod.id);
        this.applyCardSpan(cardEl, cfg.cols, cfg.rows);
      }
    }
    this.updateRowH();
  }
  /** 把「宽 cols 格 × 高 rows 格」写进卡片的 CSS 变量（grid-column span 由此驱动）。
   *  统一经过 resolveSpan 夹紧：按当前实际列数裁剪宽度（避免撑出隐式列）、
   *  按模块最低宽度（MIN_COLS）与最低宽高比（MIN_RATIO）夹紧，保证项目情况/笔记统计
   *  等关键卡片既不被压得过窄、也不会被拉成「过窄过高的竖条」。 */
  applyCardSpan(el, cols, rows) {
    var _a2;
    const modId = (_a2 = el.getAttribute("data-mod")) != null ? _a2 : "";
    const { cols: c, rows: r } = this.resolveSpan(modId, clampSpan(cols), clampSpan(rows));
    el.style.setProperty("--cols", String(c));
    el.style.setProperty("--rows", String(r));
  }
  /** 把一个（可能非法的）宽高格数解析成合法组合，渲染 / 拖拽 / 比例菜单 / 响应式夹紧共用，保证规则一致：
   *  - 夹到 1..MAX_SPAN；
   *  - 按当前实际列数裁剪宽度（2 列/1 列响应式下避免撑出隐式列）；
   *  - 按模块最低宽度（MIN_COLS）夹紧；
   *  - 按模块最低宽高比（MIN_RATIO）夹紧：宽/高 ≥ 最低比例 ⇒ 高 ≤ 宽/最低比例。 */
  resolveSpan(modId, cols, rows) {
    const board = this.boardEl;
    let colCount = board ? parseInt(board.style.getPropertyValue("--ad-cols"), 10) : 0;
    if (!(colCount > 0)) {
      colCount = board ? this.computeColCount(board.getBoundingClientRect().width, parseFloat(getComputedStyle(board).columnGap) || 12) : MAX_SPAN;
    }
    colCount = Math.max(1, Math.min(MAX_SPAN, colCount));
    let c = this.clampMinCols(modId, Math.min(colCount, clampSpan(cols)), colCount);
    let r = clampSpan(rows);
    const ratio = MIN_RATIO[modId];
    if (ratio) {
      const maxRows = Math.max(1, Math.floor(c / ratio));
      if (r > maxRows) r = maxRows;
    }
    return { cols: c, rows: r };
  }
  /** 把宽度按「模块最低宽度」与「当前实际列数」双重夹紧：响应式到更窄列数时只填充满，不强行跨列 */
  clampMinCols(modId, cols, colCount) {
    var _a2;
    const min = (_a2 = MIN_COLS[modId]) != null ? _a2 : 1;
    const c = colCount >= min ? Math.max(min, cols) : cols;
    return Math.max(1, Math.min(colCount, c));
  }
  /** 设置页修改显隐/排序后，立即重建首页（清空并重渲染全部启用模块） */
  attachBoardInteractions() {
    if (this.adBoardWired || !this.boardEl) return;
    this.adBoardWired = true;
    this.updateRowH();
    if (typeof ResizeObserver !== "undefined") {
      this.adRowHObs = new ResizeObserver(() => this.updateRowH());
      this.adRowHObs.observe(this.boardEl);
    }
    requestAnimationFrame(() => this.updateRowH());
  }
  /** 响应式布局中枢：按板面（= Obsidian 窗格）实际宽度算出列数并写入 --ad-cols，
   *  同时把 Grid 行高 --ad-row-h 锁成「单列宽」（1×1 卡正方、多列卡与 1×1 同高、比例不变）。
   *  列数走 4→3→2→1 梯度，保证每列宽度 ≥ MIN_CARD_W（可读下限），列宽仍是 1fr 随窗口等比缩放。
   *  列数变化时重夹紧全部卡片（防 2 列卡在仅剩 1 列时撑出隐式列被挤压）。 */
  updateRowH() {
    const board = this.boardEl;
    if (!board) return;
    const cs = getComputedStyle(board);
    const gap = parseFloat(cs.columnGap) || 12;
    const width = board.getBoundingClientRect().width;
    if (width <= 0) return;
    const colCount = this.computeColCount(width, gap);
    board.style.setProperty("--ad-cols", String(colCount));
    const unit = Math.max(132, (width - gap * (colCount - 1)) / colCount);
    board.style.setProperty("--ad-row-h", `${Math.round(unit)}px`);
    if (colCount !== this.adLastColCount) {
      this.adLastColCount = colCount;
      this.reapplySpans();
    }
  }
  /** 按板面实际宽度推算列数：宽→窄 4→3→2→1，每列宽度恒 ≥ MIN_CARD_W。
   *  这是「卡片不被挤压」的唯一保证——绝不能交给 CSS auto-fill（它会在宽屏生成 5~7 列，
   *  每列只有 MIN_CARD_W 那么宽，卡片内容被挤压竖排，且与 MAX_SPAN=4 的 span 模型冲突）。
   *  @param width 板面内容宽度 @param gap 列间距 */
  computeColCount(width, gap) {
    const MIN_CARD_W = 260;
    const fit = Math.floor((width + gap) / (MIN_CARD_W + gap));
    return Math.max(1, Math.min(MAX_SPAN, fit));
  }
  /** 按当前列数与各模块最低约束，用保存的 settings 比例重新夹紧所有卡片（响应式列数变化时调用） */
  reapplySpans() {
    var _a2;
    const board = this.boardEl;
    if (!board) return;
    const hm = (_a2 = this.plugin.settings.homeModules) != null ? _a2 : [];
    board.querySelectorAll(".ad-card").forEach((card) => {
      var _a3;
      const el = card;
      const modId = (_a3 = el.getAttribute("data-mod")) != null ? _a3 : "";
      const m = hm.find((x) => x.id === modId);
      if (!m) return;
      const { cols, rows } = this.resolveSpan(modId, clampSpan(m.cols), clampSpan(m.rows));
      el.style.setProperty("--cols", String(cols));
      el.style.setProperty("--rows", String(rows));
    });
  }
  async renderFavorites(board) {
    const card = this.getOrCreateCard(board, "ad-card ad-b-fav");
    this.cardHead(card, "\u2605", this.t("\u5E38\u7528\u6587\u6863", "Favorites"));
    const wrap = card.createDiv({ cls: "ad-fav" });
    const list = wrap.createDiv({ cls: "ad-fav__list" });
    const docs = await this.readDocsFile();
    if (docs.length === 0) {
      const empty = list.createDiv({ cls: "ad-fav__empty" });
      empty.createSpan({ text: this.t("\u8FD8\u6CA1\u6709\u5E38\u7528\u6587\u6863", "No favorites yet") });
      const addBtn2 = empty.createEl("button", { cls: "ad-fav__add", text: this.t("+ \u6DFB\u52A0", "+ Add") });
      addBtn2.addEventListener("click", (e) => {
        e.stopPropagation();
        void this.openFavPicker();
      });
      return;
    }
    for (const [i, doc] of docs.entries()) {
      const row = list.createDiv({ cls: "ad-fav__row" });
      row.setAttribute("data-path", doc.path);
      row.createSpan({ cls: "ad-fav__icon", text: "\u25C6" });
      row.createSpan({ cls: "ad-fav__title", text: this.favDisplayName(doc) });
      row.addEventListener("click", (e) => {
        e.stopPropagation();
        void this.openFileByPath(doc.path);
      });
      row.addEventListener("contextmenu", (e) => this.openFavMenu(e, i));
      attachTouchMenu(row, (e) => this.openFavMenu(e, i));
    }
    const addBar = wrap.createDiv({ cls: "ad-fav__addbar" });
    const addBtn = addBar.createEl("button", { cls: "ad-fav__add", text: this.t("+ \u6DFB\u52A0", "+ Add") });
    addBtn.addEventListener("click", (e) => {
      e.stopPropagation();
      void this.openFavPicker();
    });
  }
  /** 常用文档的显示名：优先自定义 title，否则用文件名（去 .md） */
  favDisplayName(doc) {
    var _a2;
    if (doc.title && doc.title.trim()) return doc.title;
    return ((_a2 = doc.path.split("/").pop()) != null ? _a2 : doc.path).replace(/\.md$/, "");
  }
  /** 打开「选择文档」模态框，把勾选的文档并入常用文档并刷新卡片 */
  async openFavPicker() {
    const existing = (await this.readDocsFile()).map((d) => d.path);
    new FavoriteDocsModal(this.app, existing, async (docs) => {
      if (!docs.length) return;
      let added = 0;
      await this.writeDocsMd((content) => {
        let cur = content;
        const seen = new Set(parseDocsMd(cur).map((p) => p.doc.path));
        for (const doc of docs) {
          if (seen.has(doc.path)) continue;
          const next = upsertDocLine(cur, doc);
          if (next) {
            cur = next;
            seen.add(doc.path);
            added++;
          }
        }
        return cur;
      });
      if (added) this.showToast(this.t("\u5DF2\u6DFB\u52A0 ", "Added ") + added + this.t(" \u4E2A\u5E38\u7528\u6587\u6863", " favorites"));
    }).open();
  }
  /** 重建「常用文档」卡片内容（getOrCreateCard 会清空并重填同一卡壳，保留比例/位置） */
  refreshFavoritesCard() {
    if (this.currentPage !== "home" || !this.boardEl) return;
    void this.renderFavorites(this.boardEl);
  }
  /** 常用文档右键菜单：打开 / 上移 / 下移 / 移除 */
  openFavMenu(e, index) {
    e.preventDefault();
    e.stopPropagation();
    const menu = new import_obsidian5.Menu();
    menu.addItem((item) => item.setTitle(this.t("\u6253\u5F00", "Open")).setIcon("file-text").onClick(() => void this.openFavAt(index)));
    menu.addItem((item) => item.setTitle(this.t("\u4E0A\u79FB", "Move up")).onClick(() => void this.moveFav(index, -1)));
    menu.addItem((item) => item.setTitle(this.t("\u4E0B\u79FB", "Move down")).onClick(() => void this.moveFav(index, 1)));
    menu.addItem((item) => item.setTitle(this.t("\u79FB\u9664", "Remove")).setIcon("trash").onClick(() => void this.removeFav(index)));
    menu.showAtMouseEvent(e);
  }
  /** 打开常用文档列表中的第 index 条 */
  async openFavAt(index) {
    const docs = await this.readDocsFile();
    const doc = docs[index];
    if (doc) void this.openFileByPath(doc.path);
  }
  /** 上移/下移常用文档并刷新 */
  async moveFav(index, delta) {
    const docs = await this.readDocsFile();
    const doc = docs[index];
    if (!doc) return;
    await this.writeDocsMd((content) => moveDocLine(content, doc.path, delta));
  }
  /** 移除一条常用文档并刷新 */
  async removeFav(index) {
    const docs = await this.readDocsFile();
    const doc = docs[index];
    if (!doc) return;
    await this.writeDocsMd((content) => removeDocLine(content, doc.path));
  }
  /* ============================================================
     待办 · 今天要处理（Obsidian Kanban 双向兼容）
     ============================================================ */
  /** 网站收藏 md：读取并解析；文件缺失时创建（旧 data.json 数据首次迁移） */
  async readSitesFile() {
    var _a;
    const path = this.plugin.settings.favoriteSitesFile;
    const f = this.app.vault.getAbstractFileByPath(path);
    if (!(f instanceof import_obsidian5.TFile)) {
      const legacy = (_a = this.plugin.settings.favoriteSites) != null ? _a : [];
      const content2 = legacy.length ? serializeSites(legacy) : "# \u7F51\u7AD9\u6536\u85CF\n\n\u8FD8\u6CA1\u6709\u6536\u85CF\u7AD9\u70B9\uFF0C\u53EF\u5728\u5361\u7247\u70B9 \uFF0B \u6DFB\u52A0\uFF0C\u6216\u76F4\u63A5\u5728\u6B64\u7F16\u8F91\uFF08## \u5206\u7C7B + - \u540D\u79F0 \u2192 URL\uFF09\u3002\n";
      await this.ensureMdFile(path, content2);
      return legacy;
    }
    const content = await this.app.vault.read(f);
    return parseSitesMd(content).map((p) => p.site);
  }
  /** 常用文档 md：读取并解析；文件缺失时创建（旧 data.json 数据首次迁移） */
  async readDocsFile() {
    var _a;
    const path = this.plugin.settings.favoriteDocsFile;
    const f = this.app.vault.getAbstractFileByPath(path);
    if (!(f instanceof import_obsidian5.TFile)) {
      const legacy = (_a = this.plugin.settings.favoriteDocs) != null ? _a : [];
      const content2 = legacy.length ? serializeDocs(legacy) : "# \u5E38\u7528\u6587\u6863\n\n\u8FD8\u6CA1\u6709\u5E38\u7528\u6587\u6863\uFF0C\u53EF\u5728\u5361\u7247\u70B9 \uFF0B \u6DFB\u52A0\uFF0C\u6216\u76F4\u63A5\u5728\u6B64\u7F16\u8F91\uFF08- [[\u8DEF\u5F84|\u663E\u793A\u540D]]\uFF09\u3002\n";
      await this.ensureMdFile(path, content2);
      return legacy;
    }
    const content = await this.app.vault.read(f);
    return parseDocsMd(content).map((p) => p.doc);
  }
  /** 网站收藏 md：行级改写后写回（不破坏手写内容）；modify 事件会自动刷新卡片 */
  async writeSiteMd(apply) {
    const path = this.plugin.settings.favoriteSitesFile;
    const f = this.app.vault.getAbstractFileByPath(path);
    if (!(f instanceof import_obsidian5.TFile)) return;
    const content = await this.app.vault.read(f);
    const next = apply(content);
    if (next == null || next === content) return;
    await this.app.vault.modify(f, next);
  }
  /** 常用文档 md：行级改写后写回 */
  async writeDocsMd(apply) {
    const path = this.plugin.settings.favoriteDocsFile;
    const f = this.app.vault.getAbstractFileByPath(path);
    if (!(f instanceof import_obsidian5.TFile)) return;
    const content = await this.app.vault.read(f);
    const next = apply(content);
    if (next == null || next === content) return;
    await this.app.vault.modify(f, next);
  }
  /** 确保 md 数据文件存在（含父目录），用于首次创建 */
  async ensureMdFile(path, content) {
    const existing = this.app.vault.getAbstractFileByPath(path);
    if (existing instanceof import_obsidian5.TFile) return existing;
    const dir = path.split("/").slice(0, -1).join("/");
    if (dir) await this.ensureFolder(dir);
    return this.app.vault.create(path, content);
  }
  /** 网站收藏 md 变化后的去抖刷新 */
  scheduleSitesRefresh() {
    if (this.sitesRefreshTimer !== null) window.clearTimeout(this.sitesRefreshTimer);
    this.sitesRefreshTimer = window.setTimeout(() => {
      this.sitesRefreshTimer = null;
      this.refreshSitesCard();
    }, 200);
  }
  /** 常用文档 md 变化后的去抖刷新 */
  scheduleDocsRefresh() {
    if (this.docsRefreshTimer !== null) window.clearTimeout(this.docsRefreshTimer);
    this.docsRefreshTimer = window.setTimeout(() => {
      this.docsRefreshTimer = null;
      this.refreshFavoritesCard();
    }, 200);
  }
  /** 读取 Kanban 数据文件并解析；文件缺失时返回空 board 与 file=null */
  async readKanbanBoard() {
    const path = this.plugin.settings.kanbanFile;
    const f = this.app.vault.getAbstractFileByPath(path);
    if (!(f instanceof import_obsidian5.TFile)) return { board: parseKanbanBoard(""), file: null };
    const content = await this.app.vault.read(f);
    return { board: parseKanbanBoard(content), file: f };
  }
  /** Kanban 文件变化后的去抖刷新（合并连续写回触发的多次 modify 事件） */
  scheduleKanbanRefresh() {
    if (this.kanbanRefreshTimer !== null) window.clearTimeout(this.kanbanRefreshTimer);
    this.kanbanRefreshTimer = window.setTimeout(() => {
      this.kanbanRefreshTimer = null;
      this.refreshKanbanCard();
    }, 200);
  }
  /** 重建「待办」卡片（仅首页） */
  refreshKanbanCard() {
    if (this.currentPage !== "home" || !this.boardEl) return;
    void this.renderKanbanTodo(this.boardEl);
  }
  /** 待办卡渲染：快速添加 + 待办列表 + 已完成折叠区 */
  async renderKanbanTodo(board) {
    const card = this.getOrCreateCard(board, "ad-card ad-b-todo");
    const summary = card.createSpan({ cls: "ad-card__hint" });
    this.cardHead(card, "\u25CE", this.t("\u5F85\u529E \xB7 \u4ECA\u5929\u8981\u5904\u7406", "To-do \xB7 Today"), void 0, summary);
    const addRow = card.createDiv({ cls: "ad-kb__addrow" });
    const input = addRow.createEl("input", { cls: "ad-kb__input", attr: { placeholder: this.t("\u5FEB\u901F\u6DFB\u52A0\u5F85\u529E\uFF0C\u56DE\u8F66\u786E\u8BA4\u2026", "Quick add, Enter to confirm\u2026") } });
    const addBtn = addRow.createEl("button", { cls: "ad-kb__addbtn", text: this.t("\u6DFB\u52A0", "Add") });
    const submit = () => {
      const text = input.value.trim();
      if (!text) return;
      void this.kanbanAdd(text);
      input.value = "";
    };
    input.addEventListener("keydown", (e) => {
      if (e.key === "Enter") submit();
    });
    addBtn.addEventListener("click", () => submit());
    const { board: kb, file } = await this.readKanbanBoard();
    if (!file) {
      card.createDiv({ cls: "ad-kb__empty", text: this.t("\u672A\u627E\u5230 Kanban \u6587\u4EF6\u300C", 'Kanban file "') + this.plugin.settings.kanbanFile + this.t("\u300D\uFF0C\u8BF7\u5728\u8BBE\u7F6E\u4E2D\u914D\u7F6E", '" not found, configure in settings') });
      return;
    }
    const all = kb.tasks;
    const pending = all.filter((t) => !t.done);
    const done = all.filter((t) => t.done);
    summary.textContent = pending.length ? pending.length + this.t(" \u9879\u5F85\u529E", " pending") : this.t("\u4ECA\u65E5\u5DF2\u6E05\u7A7A", "All cleared today");
    const body = card.createDiv({ cls: "ad-kb__body" });
    const list = body.createDiv({ cls: "ad-kb" });
    if (pending.length === 0) {
      list.createDiv({ cls: "ad-kb__empty", text: this.t("\u4ECA\u65E5\u5F85\u529E\u5DF2\u5168\u90E8\u5B8C\u6210", "All done for today") });
    } else {
      for (const t of pending) this.renderKanbanRow(list, t);
    }
    if (done.length) {
      const doneBlock = body.createDiv({ cls: "ad-kb__done" });
      const doneHead = doneBlock.createDiv({ cls: "ad-kb__donehead" });
      const caret = doneHead.createSpan({ cls: "ad-kb__caret", text: "\u25B8" });
      doneHead.createSpan({ text: `\u5DF2\u5B8C\u6210 ${done.length}` });
      const doneList = doneBlock.createDiv({ cls: "ad-kb__donelist is-collapsed" });
      for (const t of done) this.renderKanbanRow(doneList, t);
      doneHead.addEventListener("click", () => {
        doneList.classList.toggle("is-collapsed");
        caret.textContent = doneList.classList.contains("is-collapsed") ? "\u25B8" : "\u25BE";
      });
    }
  }
  /** 单行待办：勾选圆点 + 内容（双击快速编辑） */
  renderKanbanRow(list, task) {
    const row = list.createDiv({ cls: "ad-kb__row" + (task.done ? " is-done" : "") });
    const check = row.createSpan({ cls: "ad-kb__check" });
    check.addEventListener("click", (e) => {
      e.stopPropagation();
      void this.toggleKanban(task, !task.done);
    });
    row.createSpan({ cls: "ad-kb__text", text: task.content });
    row.addEventListener("dblclick", (e) => {
      e.stopPropagation();
      this.openKanbanEditor(task);
    });
    row.addEventListener("contextmenu", (e) => this.openKanbanMenu(e, task));
    attachTouchMenu(row, (e) => this.openKanbanMenu(e, task));
  }
  /** 双击快速编辑：弹出输入框修改内容，写回 Kanban 文件 */
  openKanbanEditor(task) {
    new KanbanEditModal(this.app, task.content, (text) => {
      void this.kanbanEdit(task, text);
    }).open();
  }
  /** 编辑待办内容（写回 Kanban 文件，触发自动刷新） */
  async kanbanEdit(task, text) {
    const { board, file } = await this.readKanbanBoard();
    if (!file) return;
    const next = editKanbanTask(board.rawLines.join("\n"), task, text);
    if (next !== null) await this.app.vault.modify(file, next);
  }
  /** 切换任务完成状态（写回 Kanban 文件，触发自动刷新） */
  async toggleKanban(task, done) {
    const { board, file } = await this.readKanbanBoard();
    if (!file) return;
    const next = toggleKanbanTask(board.rawLines.join("\n"), task, done, todayStr());
    if (next !== null) await this.app.vault.modify(file, next);
  }
  /** 快速添加：写入 Kanban 文件第一个列 */
  async kanbanAdd(text) {
    var _a2;
    const { board, file } = await this.readKanbanBoard();
    if (!file) {
      this.showToast(this.t("\u672A\u627E\u5230 Kanban \u6587\u4EF6\uFF0C\u8BF7\u5728\u8BBE\u7F6E\u4E2D\u914D\u7F6E", "Kanban file not found, configure in settings"), "error");
      return;
    }
    const target = (_a2 = board.columns[0]) == null ? void 0 : _a2.title;
    if (!target) {
      this.showToast(this.t("Kanban \u6587\u4EF6\u7F3A\u5C11\u5217\uFF08## \u6807\u9898\uFF09", "Kanban file has no column (## heading)"), "error");
      return;
    }
    const next = addKanbanTask(board.rawLines.join("\n"), target, text);
    if (next === null) {
      this.showToast(this.t("\u6DFB\u52A0\u5931\u8D25", "Add failed"), "error");
      return;
    }
    await this.app.vault.modify(file, next);
  }
  /** 删除任务（写回 Kanban 文件） */
  async kanbanRemove(task) {
    const { board, file } = await this.readKanbanBoard();
    if (!file) return;
    const next = removeKanbanTask(board.rawLines.join("\n"), task);
    if (next !== null) await this.app.vault.modify(file, next);
  }
  /**
   * 移动端适配：触摸设备上「长按 500ms」触发右键菜单（桌面端保留原生 contextmenu）。
   * 实现见 src/views/touchMenu.ts（共享工具）。
   */
  /** 待办行右键菜单：完成/待办切换、删除 */
  openKanbanMenu(e, task) {
    e.preventDefault();
    e.stopPropagation();
    const menu = new import_obsidian5.Menu();
    menu.addItem((item) => item.setTitle(task.done ? this.t("\u6807\u8BB0\u4E3A\u5F85\u529E", "Mark as to-do") : this.t("\u6807\u8BB0\u4E3A\u5B8C\u6210", "Mark as done")).setIcon(task.done ? "circle" : "check-circle").onClick(() => void this.toggleKanban(task, !task.done)));
    menu.addItem((item) => item.setTitle(this.t("\u5220\u9664", "Delete")).setIcon("trash").onClick(() => void this.kanbanRemove(task)));
    menu.showAtMouseEvent(e);
  }
  /* ============================================================
     天气（wttr.in，免 key）
     ============================================================ */
  /** 拉取指定城市天气，带 10 分钟缓存 */
  async fetchWeather(city) {
    var _a;
    var _a2, _b, _c, _d, _e, _f, _g, _h, _i, _j;
    if (this.weatherCache && this.weatherCache.city === city && Date.now() - this.weatherCache.at < 10 * 60 * 1e3) {
      return this.weatherCache.data;
    }
    try {
      const url = `https://wttr.in/${encodeURIComponent(city)}?format=j1&lang=zh`;
      const res = await (0, import_obsidian5.requestUrl)({ url, method: "GET", headers: { Accept: "application/json" } });
      if (res.status !== 200) return null;
      const j = res.json;
      const cur = (_a2 = j == null ? void 0 : j.current_condition) == null ? void 0 : _a2[0];
      const today = (_b = j == null ? void 0 : j.weather) == null ? void 0 : _b[0];
      if (!cur) return null;
      const code = String((_c = cur["weatherCode"]) != null ? _c : "");
      const descArr = cur["weatherDesc"];
      const meta = (_f = WEATHER_CODE[code]) != null ? _f : { icon: "\u2601", zh: String((_e = (_d = descArr == null ? void 0 : descArr[0]) == null ? void 0 : _d.value) != null ? _e : "\u5929\u6C14") };
      const data = {
        temp: String((_g = cur["temp_C"]) != null ? _g : ""),
        desc: this.plugin.settings.language === "en" ? (_a = WEATHER_EN[meta.zh]) != null ? _a : meta.zh : meta.zh,
        icon: meta.icon,
        hi: String((_h = today == null ? void 0 : today["maxtempC"]) != null ? _h : ""),
        lo: String((_i = today == null ? void 0 : today["mintempC"]) != null ? _i : ""),
        humidity: String((_j = cur["humidity"]) != null ? _j : ""),
        updated: nowFmt().slice(11)
      };
      this.weatherCache = { city, at: Date.now(), data };
      return data;
    } catch (err) {
      console.error("[Dashboard] weather fetch failed", err);
      return null;
    }
  }
  /** 天气卡渲染：当前时间实时显示 + 多城市（最多 3 个）天气块 */
  async renderWeather(board) {
    var _a2, _b;
    const card = this.getOrCreateCard(board, "ad-card ad-b-weather");
    const cities = ((_b = (_a2 = this.plugin.settings.weather) == null ? void 0 : _a2.cities) != null ? _b : []).filter(Boolean).slice(0, 3);
    const list = cities.length ? cities : ["\u65E5\u7167"];
    this.cardHead(card, "\u2600", this.t("\u5929\u6C14", "Weather"));
    const body = card.createDiv({ cls: "ad-wx" });
    const clock = body.createDiv({ cls: "ad-wx__clock" });
    const renderClock = () => {
      const n = /* @__PURE__ */ new Date();
      const p = (x) => String(x).padStart(2, "0");
      clock.textContent = `${n.getFullYear()}-${p(n.getMonth() + 1)}-${p(n.getDate())} ${p(n.getHours())}:${p(n.getMinutes())}:${p(n.getSeconds())}`;
    };
    renderClock();
    if (this.wxTimer !== null) window.clearInterval(this.wxTimer);
    this.wxTimer = window.setInterval(renderClock, 1e3);
    for (const city of list) {
      const block = body.createDiv({ cls: "ad-wx__city" });
      const head = block.createDiv({ cls: "ad-wx__cityhead" });
      head.createSpan({ cls: "ad-wx__cityname", text: city });
      const refreshBtn = head.createSpan({ cls: "ad-wx__refresh", text: "\u21BB" });
      refreshBtn.addEventListener("click", (e) => {
        e.stopPropagation();
        this.weatherCache = null;
        this.refreshWeatherCard();
      });
      const content = block.createDiv({ cls: "ad-wx__content" });
      content.createDiv({ cls: "ad-wx__loading", text: this.t("\u83B7\u53D6\u4E2D\u2026", "Loading\u2026") });
      const data = await this.fetchWeather(city);
      content.empty();
      if (!data) {
        content.createDiv({ cls: "ad-wx__err", text: this.t("\u83B7\u53D6\u5931\u8D25\uFF08\u7F51\u7EDC/\u57CE\u5E02\u540D\uFF09", "Failed (network / city name)") });
        continue;
      }
      const main = content.createDiv({ cls: "ad-wx__main" });
      main.createSpan({ cls: "ad-wx__icon", text: data.icon });
      main.createSpan({ cls: "ad-wx__temp", text: `${data.temp}\xB0` });
      main.createSpan({ cls: "ad-wx__desc", text: data.desc });
      const sub = content.createDiv({ cls: "ad-wx__sub" });
      if (data.lo || data.hi) sub.createSpan({ text: `\u4F4E ${data.lo}\xB0 / \u9AD8 ${data.hi}\xB0` });
      if (data.humidity) sub.createSpan({ text: `\u6E7F\u5EA6 ${data.humidity}%` });
      sub.createSpan({ cls: "ad-wx__time", text: this.t("\u66F4\u65B0 ", "Updated ") + data.updated });
    }
  }
  /** 重建天气卡（仅首页） */
  refreshWeatherCard() {
    if (this.currentPage !== "home" || !this.boardEl) return;
    void this.renderWeather(this.boardEl);
  }
  /* ============================================================
     日历（月历视图，复用农历函数）
     ============================================================ */
  /** 日历卡渲染 */
  renderCalendar(board) {
    const card = this.getOrCreateCard(board, "ad-card ad-b-calendar");
    const now = /* @__PURE__ */ new Date();
    if (this.calYear === 0) {
      this.calYear = now.getFullYear();
      this.calMonth = now.getMonth();
    }
    this.cardHead(card, "\u25C8", this.t("\u65E5\u5386", "Calendar"));
    this.renderCalendarBody(card);
  }
  /** 重建月历主体（切换月份时仅重建网格，保留卡壳与标题） */
  renderCalendarBody(card) {
    card.querySelectorAll(".ad-cal").forEach((el) => el.remove());
    const body = card.createDiv({ cls: "ad-cal" });
    const head = body.createDiv({ cls: "ad-cal__head" });
    const prev = head.createEl("button", { cls: "ad-cal__nav", text: "\u2039" });
    const label = head.createSpan({ cls: "ad-cal__label", text: this.plugin.settings.language === "en" ? this.calMonth + 1 + " / " + this.calYear : this.calYear + " \u5E74 " + (this.calMonth + 1) + " \u6708" });
    const next = head.createEl("button", { cls: "ad-cal__nav", text: "\u203A" });
    prev.addEventListener("click", () => {
      this.shiftCalendarMonth(-1);
      this.renderCalendarBody(card);
    });
    next.addEventListener("click", () => {
      this.shiftCalendarMonth(1);
      this.renderCalendarBody(card);
    });
    const grid = body.createDiv({ cls: "ad-cal__grid" });
    (this.plugin.settings.language === "en" ? ["Su", "Mo", "Tu", "We", "Th", "Fr", "Sa"] : ["\u65E5", "\u4E00", "\u4E8C", "\u4E09", "\u56DB", "\u4E94", "\u516D"]).forEach((d) => grid.createDiv({ cls: "ad-cal__dow", text: d }));
    const y = this.calYear, m = this.calMonth;
    const startDow = new Date(y, m, 1).getDay();
    const daysInMonth = new Date(y, m + 1, 0).getDate();
    const today = /* @__PURE__ */ new Date();
    for (let i = 0; i < startDow; i++) grid.createDiv({ cls: "ad-cal__cell is-blank" });
    for (let d = 1; d <= daysInMonth; d++) {
      const dt = new Date(y, m, d);
      const isToday = y === today.getFullYear() && m === today.getMonth() && d === today.getDate();
      const cell = grid.createDiv({ cls: "ad-cal__cell" + (isToday ? " is-today" : "") });
      cell.createDiv({ cls: "ad-cal__day", text: String(d) });
      if (this.plugin.settings.language !== "en") cell.createDiv({ cls: "ad-cal__lunar", text: getLunarDate(dt) });
    }
  }
  shiftCalendarMonth(delta) {
    let m = this.calMonth + delta;
    let y = this.calYear;
    if (m < 0) {
      m = 11;
      y--;
    }
    if (m > 11) {
      m = 0;
      y++;
    }
    this.calMonth = m;
    this.calYear = y;
  }
  /* ============================================================
     网站收藏（点击在浏览器打开）
     ============================================================ */
  /** 网站收藏卡渲染：搜索框 + 按分类分组展示 + 添加（弹窗） */
  async renderSites(board) {
    const card = this.getOrCreateCard(board, "ad-card ad-b-sites");
    this.cardHead(card, "\u25C8", this.t("\u7F51\u7AD9\u6536\u85CF", "Bookmarks"));
    const body = card.createDiv({ cls: "ad-sites" });
    const sites = await this.readSitesFile();
    const tool = body.createDiv({ cls: "ad-sites__tool" });
    const search = tool.createEl("input", { cls: "ad-sites__search", attr: { placeholder: this.t("\u641C\u7D22\u7AD9\u70B9\uFF08\u540D\u79F0/\u7F51\u5740/\u5173\u952E\u8BCD\uFF09\u2026", "Search sites (name / URL / keyword)\u2026") } });
    const addBtn = tool.createEl("button", { cls: "ad-sites__addbtn", text: "\uFF0B" });
    addBtn.setAttribute("aria-label", this.t("\u6DFB\u52A0\u7AD9\u70B9", "Add site"));
    addBtn.addEventListener("click", (e) => {
      e.stopPropagation();
      this.openSiteEditor();
    });
    const suggest = tool.createDiv({ cls: "ad-sites__suggest" });
    const list = body.createDiv({ cls: "ad-sites__list" });
    const render = () => {
      list.empty();
      const q = search.value.trim().toLowerCase();
      const filtered = sites.filter((s) => {
        var _a, _b;
        if (!q) return true;
        return [s.name, s.url, (_a = s.category) != null ? _a : "", (_b = s.keywords) != null ? _b : ""].join(" ").toLowerCase().includes(q);
      });
      if (filtered.length === 0) {
        list.createDiv({ cls: "ad-sites__empty", text: sites.length ? this.t("\u6CA1\u6709\u5339\u914D\u7684\u7AD9\u70B9", "No matching sites") : this.t("\u8FD8\u6CA1\u6709\u6536\u85CF\u7AD9\u70B9\uFF0C\u70B9 \uFF0B \u6DFB\u52A0", "No bookmarks yet, tap \uFF0B to add") });
        return;
      }
      const groups = /* @__PURE__ */ new Map();
      for (const s of filtered) {
        const cat = s.category && s.category.trim() ? s.category.trim() : this.t("\u672A\u5206\u7C7B", "Uncategorized");
        if (!groups.has(cat)) groups.set(cat, []);
        groups.get(cat).push(s);
      }
      for (const [cat, items] of groups) {
        const collapsed = this.sitesCollapsed.has(cat);
        const group = list.createDiv({ cls: "ad-sites__group" + (collapsed ? " is-collapsed" : "") });
        const catEl = group.createDiv({ cls: "ad-sites__cat", text: cat });
        catEl.setAttribute("title", collapsed ? this.t("\u70B9\u51FB\u5C55\u5F00", "Expand") : this.t("\u70B9\u51FB\u6298\u53E0", "Collapse"));
        catEl.addEventListener("click", () => {
          if (this.sitesCollapsed.has(cat)) this.sitesCollapsed.delete(cat);
          else this.sitesCollapsed.add(cat);
          render();
        });
        if (!collapsed) {
          for (const s of items) this.renderSiteRow(group, s, sites.indexOf(s));
        }
      }
    };
    let suggestTimer = null;
    let sgIndex = -1;
    const closeSuggest = () => {
      suggest.removeClass("is-open");
      suggest.empty();
      sgIndex = -1;
    };
    const renderSuggest = () => {
      suggest.empty();
      const q = search.value.trim().toLowerCase();
      if (!q) {
        suggest.removeClass("is-open");
        return;
      }
      const hits = sites.filter((s) => {
        var _a, _b;
        return [s.name, s.url, (_a = s.category) != null ? _a : "", (_b = s.keywords) != null ? _b : ""].join(" ").toLowerCase().includes(q);
      }).slice(0, 8);
      if (!hits.length) {
        suggest.removeClass("is-open");
        return;
      }
      suggest.addClass("is-open");
      hits.forEach((s) => {
        const item = suggest.createDiv({ cls: "ad-sites__sg" });
        item.createSpan({ cls: "ad-sites__sg-icon", text: s.icon || "\u25C8" });
        item.createSpan({ cls: "ad-sites__sg-name", text: s.name || s.url });
        if (s.category) item.createSpan({ cls: "ad-sites__sg-cat", text: s.category });
        item.addEventListener("mousedown", (e) => {
          e.preventDefault();
          search.value = s.name || s.url;
          search.dispatchEvent(new Event("input"));
          closeSuggest();
          search.focus();
        });
      });
    };
    search.addEventListener("input", () => {
      render();
      if (suggestTimer !== null) window.clearTimeout(suggestTimer);
      suggestTimer = window.setTimeout(renderSuggest, 120);
    });
    search.addEventListener("keydown", (e) => {
      var _a, _b;
      const items = suggest.querySelectorAll(".ad-sites__sg");
      if (e.key === "ArrowDown" || e.key === "ArrowUp") {
        if (!items.length) return;
        e.preventDefault();
        sgIndex = e.key === "ArrowDown" ? (sgIndex + 1) % items.length : (sgIndex - 1 + items.length) % items.length;
        items.forEach((el, i) => el.toggleClass("is-active", i === sgIndex));
      } else if (e.key === "Enter" && sgIndex >= 0 && items[sgIndex]) {
        e.preventDefault();
        const active = items[sgIndex];
        const name = (_b = (_a = active.querySelector(".ad-sites__sg-name")) == null ? void 0 : _a.textContent) != null ? _b : "";
        search.value = name.trim();
        search.dispatchEvent(new Event("input"));
        closeSuggest();
      } else if (e.key === "Escape") {
        closeSuggest();
      }
    });
    search.addEventListener("blur", () => {
      window.setTimeout(closeSuggest, 150);
    });
    render();
  }
  /** 单个站点行：图标（emoji 或 favicon）+ 名称，点击打开 */
  renderSiteRow(group, site, index) {
    const tile = group.createDiv({ cls: "ad-sites__tile" });
    tile.setAttribute("title", site.url);
    const display = site.name && site.name.trim() ? site.name.trim() : site.url;
    const first = display ? String(display).charAt(0).toUpperCase() : "\u25C6";
    tile.createDiv({ cls: "ad-sites__tile-icon", text: first });
    tile.createDiv({ cls: "ad-sites__tile-name", text: display });
    tile.addEventListener("click", () => window.open(site.url, "_blank", "noopener"));
    tile.addEventListener("contextmenu", (e) => this.openSiteMenu(e, index));
    attachTouchMenu(tile, (e) => this.openSiteMenu(e, index));
  }
  /** 新增 / 编辑站点弹窗；保存后写回 settings 并刷新 */
  openSiteEditor(existing, index = -1) {
    new SiteEditModal(this.app, existing != null ? existing : {}, async (data) => {
      const entry = {
        name: data.name,
        url: data.url,
        category: data.category || void 0,
        keywords: data.keywords || void 0
      };
      const oldUrl = existing ? existing.url : void 0;
      await this.writeSiteMd((content) => {
        if (!oldUrl) {
          const sites = parseSitesMd(content).map((p) => p.site);
          if (sites.some((s) => s.url === entry.url)) {
            this.showToast(this.t("\u8BE5\u7AD9\u70B9\u5DF2\u5728\u6536\u85CF\u4E2D", "This site is already bookmarked"));
            return null;
          }
          return upsertSiteLine(content, entry);
        }
        if (oldUrl === entry.url) return upsertSiteLine(content, entry);
        let cur = removeSiteLine(content, oldUrl);
        if (cur == null) cur = content;
        return upsertSiteLine(cur, entry);
      });
    }).open();
  }
  hostOf(url) {
    try {
      return new URL(url).host;
    } catch (e) {
      return url;
    }
  }
  /** 站点右键菜单：打开 / 编辑 / 上移 / 下移 / 移除 */
  openSiteMenu(e, index) {
    e.preventDefault();
    e.stopPropagation();
    const menu = new import_obsidian5.Menu();
    menu.addItem((item) => item.setTitle(this.t("\u6253\u5F00", "Open")).setIcon("globe").onClick(() => void this.openSiteAt(index)));
    menu.addItem((item) => item.setTitle(this.t("\u7F16\u8F91", "Edit")).setIcon("pencil").onClick(() => void this.editSiteAt(index)));
    menu.addItem((item) => item.setTitle(this.t("\u4E0A\u79FB", "Move up")).onClick(() => void this.moveSite(index, -1)));
    menu.addItem((item) => item.setTitle(this.t("\u4E0B\u79FB", "Move down")).onClick(() => void this.moveSite(index, 1)));
    menu.addItem((item) => item.setTitle(this.t("\u79FB\u9664", "Remove")).setIcon("trash").onClick(() => void this.removeSite(index)));
    menu.showAtMouseEvent(e);
  }
  /** 打开网站收藏列表中的第 index 条 */
  async openSiteAt(index) {
    const sites = await this.readSitesFile();
    const site = sites[index];
    if (site) window.open(site.url, "_blank", "noopener");
  }
  /** 编辑网站收藏列表中的第 index 条 */
  async editSiteAt(index) {
    const sites = await this.readSitesFile();
    const site = sites[index];
    if (site) this.openSiteEditor(site, index);
  }
  async moveSite(index, delta) {
    const sites = await this.readSitesFile();
    const site = sites[index];
    if (!site) return;
    await this.writeSiteMd((content) => moveSiteLine(content, site.url, delta));
  }
  async removeSite(index) {
    const sites = await this.readSitesFile();
    const site = sites[index];
    if (!site) return;
    await this.writeSiteMd((content) => removeSiteLine(content, site.url));
  }
  /** 重建网站收藏卡（仅首页） */
  refreshSitesCard() {
    if (this.currentPage !== "home" || !this.boardEl) return;
    void this.renderSites(this.boardEl);
  }
  /* ---- Shared card header ---- */
  cardHead(card, icon, title, hint, hintEl) {
    const head = card.createDiv({ cls: "ad-card__head" });
    const h3 = head.createEl("h3", { cls: "ad-card__title" });
    h3.createSpan({ cls: "ad-marker", text: icon });
    h3.appendText(title);
    if (hintEl) head.appendChild(hintEl);
    else if (hint) head.createSpan({ cls: "ad-card__hint", text: hint });
  }
};
/* ---- 番茄钟（Pomodoro） ---- */
/** 环形进度环半径（与 SVG viewBox 内 circle r 保持一致） */
_DashboardView.POMO_R = 52;
var DashboardView = _DashboardView;

// src/main.ts
var Dashboard = class extends import_obsidian6.Plugin {
  async onload() {
    await this.loadSettings();
    this.registerView(VIEW_TYPE, (leaf) => new DashboardView(leaf, this));
    this.addRibbonIcon("layout-dashboard", "Dashboard", () => {
      void this.activateView();
    });
    this.addCommand({
      id: "open-dashboard",
      name: "Open dashboard",
      callback: () => {
        void this.activateView();
      }
    });
    this.addSettingTab(new DashboardSettingTab(this.app, this));
  }
  onunload() {
  }
  async loadSettings() {
    var _a;
    const loaded = (_a = await this.loadData()) != null ? _a : {};
    const storedLayoutVersion = typeof loaded.homeLayoutVersion === "number" ? loaded.homeLayoutVersion : 0;
    this.settings = Object.assign({}, DEFAULT_SETTINGS, loaded);
    for (const key of ["quickCapture", "diary"]) {
      const grp = loaded[key];
      if (grp && grp.templateFolder && grp.templateFile && !grp.templateFile.includes("/") && !grp.templateFile.endsWith(".md")) {
        this.settings[key].templateFile = `${grp.templateFolder}/${grp.templateFile}`;
      }
    }
    this.normalizeHomeModules(storedLayoutVersion);
    this.normalizeWeatherSettings(loaded.weather);
  }
  /**
   * 迁移天气设置：旧版存单个 city 字符串，新版为 cities 数组（最多 3 个）。
   * 旧数据 `{ city: '日照' }` 或非法值 → 转成 `{ cities: ['日照'] }` 并落盘。
   */
  normalizeWeatherSettings(loaded) {
    const w = this.settings.weather;
    let cities = [];
    if (loaded && Array.isArray(loaded.cities) && loaded.cities.length > 0) {
      cities = loaded.cities.map((c) => typeof c === "string" ? c.trim() : "").filter(Boolean);
    } else if (loaded && typeof loaded.city === "string" && loaded.city.trim()) {
      cities = [loaded.city.trim()];
    }
    cities = cities.slice(0, 3);
    if (!cities.length) cities = ["\u65E5\u7167"];
    const changed = !Array.isArray(w == null ? void 0 : w.cities) || w.cities.length !== cities.length || w.cities.some((c, i) => c !== cities[i]);
    if (changed) {
      this.settings.weather = { cities };
      void this.saveSettings();
    }
  }
  /**
   * 归一化 + 迁移首页模块布局，保证 homeModules 始终是一份完整可用的数据：
   * 1. 缺失/损坏 → 直接用默认布局；
   * 2. 补齐新增模块（老 data.json 不含新卡片时不会「丢卡」）；
   * 3. 修正非法的 cols/rows/order/enabled；
   * 4. 版本迁移：storedVersion < HOME_LAYOUT_VERSION 时，把 cols/rows 重置为最新默认值
   *    （保留用户的显隐与排序）。此前比例功能存在 bug 从未真正落盘，故一次性纠正是安全的。
   */
  normalizeHomeModules(storedVersion) {
    var _a, _b;
    const defaults = new Map(DEFAULT_HOME_MODULES.map((m) => [m.id, m]));
    let hm = this.settings.homeModules;
    let changed = false;
    if (!Array.isArray(hm) || hm.length === 0) {
      hm = DEFAULT_HOME_MODULES.map((m) => ({ ...m }));
      this.settings.homeModules = hm;
      changed = true;
    }
    for (const d of DEFAULT_HOME_MODULES) {
      if (!hm.some((m) => m.id === d.id)) {
        hm.push({ ...d, order: hm.length });
        changed = true;
      }
    }
    const migrate = storedVersion < HOME_LAYOUT_VERSION;
    for (const m of hm) {
      const d = defaults.get(m.id);
      const dc = (_a = d == null ? void 0 : d.cols) != null ? _a : 1;
      const dr = (_b = d == null ? void 0 : d.rows) != null ? _b : 1;
      if (migrate && d) {
        if (m.cols !== dc || m.rows !== dr) {
          m.cols = dc;
          m.rows = dr;
          changed = true;
        }
      }
      if (typeof m.cols !== "number" || !Number.isFinite(m.cols) || m.cols < 1 || m.cols > 4) {
        m.cols = dc;
        changed = true;
      }
      if (typeof m.rows !== "number" || !Number.isFinite(m.rows) || m.rows < 1 || m.rows > 4) {
        m.rows = dr;
        changed = true;
      }
      if (typeof m.order !== "number" || !Number.isFinite(m.order)) {
        m.order = 0;
        changed = true;
      }
      if (typeof m.enabled !== "boolean") {
        m.enabled = true;
        changed = true;
      }
    }
    const sorted = [...hm].sort((a, b) => a.order - b.order);
    sorted.forEach((m, i) => {
      if (m.order !== i) {
        m.order = i;
        changed = true;
      }
    });
    if (this.settings.homeLayoutVersion !== HOME_LAYOUT_VERSION) {
      this.settings.homeLayoutVersion = HOME_LAYOUT_VERSION;
      changed = true;
    }
    if (changed) void this.saveSettings();
  }
  /** 恢复首页默认布局（显隐 / 顺序 / 比例全部回到默认） */
  async resetHomeLayout() {
    this.settings.homeModules = DEFAULT_HOME_MODULES.map((m) => ({ ...m }));
    this.settings.homeLayoutVersion = HOME_LAYOUT_VERSION;
    await this.saveSettings();
  }
  async saveSettings() {
    await this.saveData(this.settings);
  }
  /**
   * Switch Obsidian's own light/dark appearance.
   *
   * `vault.setConfig('theme', ...)` is an internal (undocumented) API — it is the
   * only way to drive the global appearance from a plugin, so it is called
   * defensively and the body classes are updated as a fallback in case the
   * internal call is missing or renamed in a future Obsidian release.
   */
  setObsidianTheme(mode) {
    var _a;
    try {
      const vault = this.app.vault;
      (_a = vault.setConfig) == null ? void 0 : _a.call(vault, "theme", mode === "light" ? "moonstone" : "obsidian");
    } catch (err) {
      console.error("[Dashboard] failed to set Obsidian theme", err);
    }
    document.body.classList.toggle("theme-light", mode === "light");
    document.body.classList.toggle("theme-dark", mode === "dark");
    this.app.workspace.trigger("css-change");
  }
  /** Current effective Obsidian appearance. */
  currentObsidianTheme() {
    return document.body.classList.contains("theme-light") ? "light" : "dark";
  }
  /** Refresh the header theme toggle (icon + tooltip) in every open dashboard view. */
  refreshThemeButtons() {
    for (const leaf of this.app.workspace.getLeavesOfType(VIEW_TYPE)) {
      const view = leaf.view;
      if (view instanceof DashboardView) view.refreshThemeButton();
    }
  }
  /** Push the current custom-title setting into any open dashboard view. */
  refreshDashboardTitle() {
    for (const leaf of this.app.workspace.getLeavesOfType(VIEW_TYPE)) {
      const view = leaf.view;
      if (view instanceof DashboardView) view.refreshTitle();
    }
  }
  /** 设置页修改首页模块显隐/排序后，立即重建所有已打开的仪表盘首页 */
  refreshHome() {
    for (const leaf of this.app.workspace.getLeavesOfType(VIEW_TYPE)) {
      const view = leaf.view;
      if (view instanceof DashboardView) view.rebuildHome();
    }
  }
  async activateView() {
    const existing = this.app.workspace.getLeavesOfType(VIEW_TYPE);
    if (existing.length > 0 && existing[0]) {
      void this.app.workspace.revealLeaf(existing[0]);
      return;
    }
    const leaf = this.app.workspace.getLeaf("tab");
    if (!leaf) return;
    await leaf.setViewState({ type: VIEW_TYPE, active: true });
    void this.app.workspace.revealLeaf(leaf);
  }
};
